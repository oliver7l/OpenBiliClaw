# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.worktrees/

# Testing
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/

# OS
.DS_Store
Thumbs.db

# Project-specific
data/
*.db
*.sqlite
*.sqlite3

# User data (never commit)
user_profiles/
memory_store/
behavior_logs/
# 用户抓取的小红书媒体（阅读库离线导入产物，个人内容、非源码）
images/xhs/
# 已读库归档正文（archive_zhihu_readlib.py 写到仓库根 notes/），个人内容、非源码
/notes/

# Secrets / local config (never commit)
config.toml
config.toml.*
.env
.env.local

# Node (Chrome extension)
extension/node_modules/
extension/dist/
extension/openbiliclaw-extension-*.zip

# Raw screenshots (canonical copies live in docs/images/)
screenshot/

# Local artifacts / debug output
output/
.codex/
# Scratch dir for local debug screenshots + serve-api.log, and stray
# root-level probe screenshots from manual UI verification runs
.tmp/
/probe-*.png

# Local AI / chat tooling state — runtime attachments (cc-connect),
# session memory + project plans (Claude Code). Per-developer, never
# part of the project source.
.cc-connect/
.claude/

# Local dev caches
.deps/
.playwright-mcp/

# Logs
*.log
logs/
extension/dist-firefox/

# WorkBuddy project data (automations, memory) — per-developer, never commit
.workbuddy/

# Runtime locks & migration state
*.lock
.openbiliclaw-migration/

# Local test artifacts
.pytest-tmp/

# 克隆系统运行时生成的站点数据（勿提交）
src/openbiliclaw/web/clone/sites/

# 乐仔成长相册缩略图（个人照片数据，本地保留不入库；
# 由 `python -m openbiliclaw.lezai --apply` 从外部源库同步进来）
src/openbiliclaw/web/lezai/thumbs/

# 求职面试知识库：01 原始资料（13G）保持本地不入库，
# 其余（02 方向/03 岗位弹药/系统引擎脚本·CSV·规范·总索引）纳入版本控制
求职知识库/01_原始资料库/
# 参考项目（含嵌套 .git，本地保留，不进版本控制）
references/agent-interview-hub/
references/my-interview/

# 全部参考项目（第三方代码/个人资料，本地保留，不进版本控制）
# 非锚定规则 → 同时覆盖 12_开源项目研究/references/（2026-09-18 迁入）
references/
# 个人面试转录等隐私材料
transcript/

# 知识库数据库备份（*.db.bak-* 不被 *.db 规则覆盖，必须单独忽略）
*.db.bak*
*.db-wal
*.db-shm

# 临时/草稿产物（本地保留，不入库）
/_scripts/
/draft_cb610c54_folder/

# 个人面试准备笔记（大文件，本地保留，不入库）
求职知识库/03_岗位弹药库/GoodLuckStudio-面试准备/
求职知识库/03_岗位弹药库/万联易达-面试准备/
求职知识库/03_岗位弹药库/定制简历/
/04_比亚迪/
求职知识库/03_岗位弹药库/深圳灵动-面试准备/

# Cookie / 敏感信息
data/cookies/
data/*cookie*.json
data/*cookies*.txt
config.toml

# 知识库引擎 SQLite 备份文件（revbak_* 单个 400MB+，禁止入库；历史已于 2026-09-13 清洗）
*.revbak_*

# 简历库（个人求职资料，绝不入库）
/简历库/

# 二创项目与参考克隆（各自独立 git，勿被外层跟踪）
二创/
references/

# 移动端定制构建源码（独立 git 仓库，勿入主仓库；2026-09-18 编号 15_，14 已被微信小店占用）
15_mobile/

# 乐仔相册微信小程序：生成物含**媒体签名**与个人照片路径，本地生成不入库
# （跑 `python scripts/build_album_miniprogram.py` 重新产出）
miniprogram-album/data/photos.js
miniprogram-album/utils/config.js

# 个人相册库（2026-09-18 自 009-暂存内容 迁入项目根，共 ~127G 私人照片/视频，整目录绝不入库）
# 含 _photo_index/ 下的索引库、缩略图、CLIP 向量、专用 venv、工具脚本
/07_相册/
/08_乐仔相册/
/09_QQ相册/

# QQ 相册导出·按小孩人脸分组（2026-09-18 自 009-暂存内容 迁入；10204 张含未成年人 PII，绝不入库）
/11_qq相册导出-中三班-小孩儿分组/

# 开源项目研究模块（2026-09-18 自 references/ + data/ + scripts/oss_research/ 迁入项目根统一编号）
#   · 12_开源项目研究/references/  ——第三方克隆（772M），已由下方 references/ 规则忽略
#   · 12_开源项目研究/oss_research.db(+ .bak)——派生数据，已由 *.db / *.db.bak* 规则忽略
#   · 12_开源项目研究/scripts/backfill.py ——种子回填脚本（代码），保持入库跟踪
# 故此处不做整目录忽略，避免误伤 backfill.py。

# 小火车英语启蒙（2026-09-18 自 002-探索项目/041-KidsEnglish 迁入统一编号）
#   · 成品/原片 mp4（约 1G）不入库；build/ 只留构建脚本，其余（yt-dlp 下载残留、TTS 中间产物）忽略
#   · build 脚本、播放器 HTML、方案 md 照常入库
/13_KidsEnglish/**/*.mp4
# 上方全局 build/ 规则会把目录本身也排除（目录被排除后其内容无法被 ! 救回），
# 故先重新包含本目录，再排除其中非脚本内容：
!13_KidsEnglish/build/
13_KidsEnglish/build/*
!/13_KidsEnglish/build/*.py

# 根级杂项产物（2026-09-18 清理盘点时补）
#   · 简历库打包备份（8.2MB，个人求职资料，同 /简历库/ 口径）
#   · 下载残留的空归档、WorkBuddy 会话元数据文件
/简历库_backup_*.tar.gz
/master.tar.gz
/.wbapp_*.genie

# 大照片/模型/独立子项目（2026-09-19 盘库补：不入库，避免撑爆仓库 / 同类大文件请自行 gitign）
#   · 19_统一相册库：约 116GB 原始照片分组
#   · 照片人物判定：人脸识别模型二进制（.onnx/.pkl，约 767M）
#   · tools/：独立子项目（含各自 .git / .env 密钥 / .venv / node_modules），单独管理，不与主仓混
/19_统一相册库/
/照片人物判定/
/18_照片分组/
/tools/
# 二创用途视图（机内软链入口，见 scripts/vendor_manage.py scan --usage；不入库）
/_应用/
/_调研/
#   · 03_万声科技 内嵌的 GitHub 仓库存档（外来仓库，作 gitlink 会污染本仓；保留其面试文档，仅排除归档仓）
03_万声科技/**/GitHub仓库存档/
