"""拼多多主管面业务题「砍一刀动态定价（基于 CAC 的动态定价模型）」详细话术入库。

直接写 SQLite，不依赖 LLM。
撤销：DELETE FROM interview_scripts WHERE source = '业界调研-拼多多主管面-砍一刀';
"""
from __future__ import annotations

import sqlite3
from datetime import datetime
from pathlib import Path

DB = Path(__file__).resolve().parents[1] / "data" / "interview.db"
NOW = datetime.now().isoformat(timespec="seconds")

CONTENT = """## 标准回答（主管面 30 秒版，先给结论）

砍一刀的本质**不是"随机砍价"，而是在 CAC（获客成本）上限约束下的动态定价 + 社交裂变增长引擎**。

一句话：平台愿意为"免费领手机"这种新客付一个获客成本 X 元，算法要做的是——**保证用户拉来的人头价值 ≥ 商品成本，且活动整体的边际 CAC ≤ 平台设定的 CPA 上限**。

所以这不是"能不能砍完"的问题，而是"**让谁砍完、让谁永远差一刀**"的预算分配问题。技术只是实现商业目标的手段。

---

## 一、先把题定性：这是业务题，不是纯算法题（主管面最想听这层）

三句话定位：
1. **商业目标**：把传统投放 150–300 元/人的获客成本，用"社交裂变"压到几十元甚至更低——成本从"平台掏钱"变成"用户消耗自己的社交关系"。
2. **目标函数**（务必说出来，主管面就考你有没有目标意识）：
   - `max 增量新客数 s.t. 活动总成本 ≤ 预算上限`，且 `边际 CAC ≤ CPA 上限`
   - 等价写法：`max 拉新量 s.t. LTV/CAC ≥ 3` 或 `ROI = 增量毛利/活动成本 > 1`
3. **手段**：用户价值加权定价 + 进度条概率控制 + 风控去噪 + 单位置换延长留存。

**关于指标口径**：分母必须是"**增量**新客"——要用 holdout（对照组）或 PSM/uplift 口径剔除"本来就会下载的人"（cannibalization 自食效应），否则增长数字是虚的。这是主管面和普通技术面的分水岭。

---

## 二、算法层：不是随机，是"看人下菜碟"的价值加权定价

**核心公式**：`本次砍掉金额 = 剩余金额 × 衰减系数 × 用户价值权重`

1. **用户价值权重**（同一次砍价，不同人砍出来的钱完全不同）
   | 帮砍人类型 | 权重 | 实际砍掉 |
   |---|---|---|
   | 新用户（能带来新增） | 10.0 | 约 20 元 |
   | 老用户（已无拉新价值） | 0.1 | 约 0.2 元 |
   | 黑产/羊毛党 | 0.001 | 约 0.01 元 |
   - 这就是"新用户一刀 20 元、老用户只砍 0.01 元"的根因。权重可由模型估：**新客概率 × 后续 LTV 预估**（本质是给每个帮砍行为估一个"拉新价值分"）。

2. **进度收敛（"芝诺悖论"）**：临近 99% 时切等比衰减，1 元被切成 0.5+0.25+0.125+…，**无限趋近 0 但永不等于 0**。
   - 实测量级参考：剩余 0.0003573%、人均砍掉 0.00005% → 还需约 **7146 个新用户**才能到 100%。

3. **动态阈值上浮（真正的"动态定价"）**：成功率阈值随**实时拉新成本**浮动，直到 `边际获客成本 ≤ 平台 CPA 上限` 为止——成本高了就"吞刀"给你看"福气满满"，成本低了就放水让你成功。

4. **多状态机建模**：把砍价抽象成马尔可夫链，状态空间 5 关（红包 → 钻石 → 拼图 → 吞刀 → 锦鲤）。最后那个"锦鲤"态就是**止损阀**：一旦检测到新增人数已无法覆盖"商品成本 + ROI 要求"，直接把成功概率调 0。

> 面试话术：**"我会把砍价建模成马尔可夫决策过程，每个状态的动作是'给定价值权重后的金额分配'，奖励是'增量新客 × 单位价值 − 补贴成本'，约束是累计边际 CAC ≤ 上限。"**

---

## 三、架构层：算法保证"不亏"，架构保证"不崩"

| 问题 | 坑 | 解法 |
|---|---|---|
| 金额精度 | Redis 存 float/String 又慢又丢精度 | **去小数点化**：1 元 = 1,000,000 微，Redis 存 Long，Lua `DECRBY` 纳秒级且精确 |
| 大 V 热 Key | 一条链接瞬间涌入百万人（100 万 QPS 打爆单 Key） | **JVM LocalCache 前置**拦 90% 无效/重复查询；写入走 **MQ 削峰填谷**串行化，前端只显示"砍价中…" |
| 并发一致性 | 超卖/重复减 | Redis 原子 Lua 脚本 + 幂等键（用户 × 活动 × 轮次） |

一句话：**"金额用 Long 存微、热点本地缓存、写请求过 MQ"**，这三个点说出来就能证明你真做过高并发。

---

## 四、风控层：与黑产的博弈（主管面最想听的"风险意识"）

1. **设备指纹 + 行为生物学**：不只看 IP——看陀螺仪抖动、按压面积变化、操作间隔分布。真人手指按压面积是变化的、有物理抖动；脚本数据死板。
2. **风控降权（静默失败）**：判定为机器的，前端照样提示"砍价成功"，后台实际减 0 元——**不打草惊蛇**。
3. **关键节点强制图灵测试**：在"最后 1 元""最后 0.01 元"这种临门一脚弹滑动拼图/选字验证码，废掉绝大多数自动化脚本。
4. **同 IP/同设备聚集检测**：检测到同一 IP 多个账号即触发"吞刀"。
5. **兜底**：羊毛党占比红线 < 10%，超限就降奖励或抬门槛。

> 主管面加分句：**"风控本质是成本控制——诈骗成本一旦超过 CPA 上限，整个活动的经济模型就崩了，所以风控是一等公民，不是附属。"**

---

## 五、产品层：单位置换（为什么最后变成"金币/碎片"）

后端金额已精确到 1 微（0.000001 元），但前端只能显示 2 位小数——继续砍数字不动，看起来像 Bug。
于是做**单位置换**：还差 0.01 元 → 抽到"10 金币"（100 金币 = 0.01 元）→ 还差 1 金币 → 抽到"1 碎片"（10 碎片 = 1 金币）。
通过不断贬值货币单位，**进度条被无限拉长，用户永远"只差一点点"**——既规避技术尴尬，又合法延长留存。

---

## 六、增长侧的关键指标（讲指标体系，主管会觉得你懂业务）

| 类别 | 指标 | 计算 | 参考值 |
|---|---|---|---|
| 拉新 | 增量新客数（剔除羊毛党） | 活动新客 − 自然新增 | 分母口径要对 |
| 成本 | 单人 CAC | 活动总成本 ÷ 增量新客 | **< LTV 的 1/3** |
| 裂变 | 病毒系数 K | 人均带来的新客数 | >1 自增长，>2 优秀 |
| 留存 | 新客 7 日留存 | 7 天后仍留存 ÷ 新客 | >20% 优秀 |
| 效益 | 活动 ROI | 增量毛利 ÷ 活动成本 | >1 合格，>3 优秀 |
| 风险 | 羊毛党占比 / 投诉率 | — | <10% / <0.1% |

激励设计的经验红线：**激励成本 ≤ 首单毛利的 30%**，超过就必须靠复购回本；用阶梯奖励（拉 1/3/5 人分级）控单人成本上限。

**业界量级参考**：拼多多 2025Q1 销售费用 334 亿（同比 +43%），但平均每用户获客成本约 2.4 美元、**比行业低约 80%**——这就是裂变 vs 投放的差距。每分享一次平均触达 23 人、其中 3.2 人转化为新用户；超级节点（PageRank Top1%）平均好友 1300+，助力回流率 38%（普通用户仅 7%）。

---

## 七、和你（推荐算法岗）的结合点——把话题拉回主场

面试官问这道题，本质是看你能不能"从业务目标倒推算法设计"。可以主动接一句：
- **用户价值分**：给每个帮砍人估 LTV/新客概率，作为定价权重——这和你做精排多目标、LTV 进排序是同一套思路（可衔接 B3 话术）。
- **增量口径**：评估活动效果要用 uplift（T-learner/DR-learner）而非相关性，和 C1 发券 uplift 是同一个工具箱。
- **A/B 与实验**：分层正交、护栏指标，别让活动污染主站实验。
- **风控模型**：异常检测 / 图算法（助力关系图 430 万节点、1.9 亿边，PageRank 抽超级节点）。

---

## 八、追问应对

- **Q：为什么不做真随机？** → 真随机方差极大，前 10 人可能每人砍 200 把手机白送；且无法保证总成本可控。要做的是"期望受控的定价"。
- **Q：怎么保证不亏？** → 目标函数带约束：`累计补贴成本 ≤ 商品成本`，且 `边际 CAC ≤ CPA 上限`；逼近上限就吞刀/收敛。
- **Q：用户投诉/法律风险？** → 规则披露做在协议里（真实判例：2022 年用户起诉，法院按"侵犯知情权"判赔 400 元，未认定欺诈）。所以要在"用户感知"和"法律合规"之间留安全边际，别把规则写得太具体也别太模糊。
- **Q：怎么评估活动做得好不好？** → 只看新增人数会被自己骗，必须用 holdout 看**增量**，再看 CAC、7 日留存、LTV/CAC、ROI 和羊毛党占比。
- **Q：如果预算固定，怎么分配？** → 这是个优化问题：在 CAC 约束下把补贴分给"边际增量最高"的人群（uplift 排序），而不是人人均摊。

## 参考资料
- B站 BV1PnKm6dEi1《拼多多二面：设计砍一刀算法》程序员创作家
- 情报汇总_拼多多推荐算法_第一轮.md（砍一刀算法题定位）
- 搜狐《拼多多"砍一刀"：数据分析劈出的千亿市场》（动态概率 + 在线优化）
- 网易《拼多多员工自述：算法如何精准收割你的每一分钱》（维度/成本数据）
- 今日头条《砍一刀永远差 0.01%，是运气问题还是设计陷阱？》（法律判例）
- Temu 二面场景题拆解（送手机：成本 2000 / 愿付 CAC 200）+ 面试标准回答模板
"""

KEY_POINTS = (
    "本质=CAC约束下的动态定价+社交裂变，不是随机砍价；"
    "目标函数 max增量新客 s.t. 边际CAC≤CPA上限 / LTV/CAC≥3；"
    "算法=用户价值权重(新10/老0.1/黑产0.001)×等比衰减(芝诺悖论)×阈值动态上浮；"
    "架构=Long存微+Redis Lua+LocalCache防热Key+MQ削峰；"
    "风控=行为探针(陀螺仪/按压面积)+关键节点验证码+静默降权+吞刀；"
    "产品=单位置换(金额→金币→碎片)；"
    "评估=holdout看增量、CAC<LTV/3、K>1、7日留存>20%、羊毛党<10%"
)

TITLE = "砍一刀动态定价（拼多多主管面 · 基于 CAC 的动态定价模型）"

conn = sqlite3.connect(DB)
cur = conn.cursor()

# 幂等：先删同源旧记录
cur.execute("DELETE FROM interview_scripts WHERE source = ?", ("业界调研-拼多多主管面-砍一刀",))

cur.execute(
    """
    INSERT INTO interview_scripts
      (type, company, position, title, content, key_points, priority, tags,
       used_count, note, source, created_at, updated_at)
    VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?)
    """,
    (
        "技术问答",
        "拼多多",
        "推荐算法（主管面）",
        TITLE,
        CONTENT,
        KEY_POINTS,
        "高",
        "拼多多,主管面,业务设计,砍一刀,动态定价,CAC,裂变增长,uplift,ROI",
        0,
        "主管面业务/系统设计题。答题主线：先定业务性质→目标函数→算法→架构→风控→指标体系→拉回推荐主场。",
        "业界调研-拼多多主管面-砍一刀",
        NOW,
        NOW,
    ),
)
new_id = cur.lastrowid
conn.commit()

print(f"inserted id={new_id}")
row = cur.execute(
    "SELECT id,type,company,title,priority,length(content) FROM interview_scripts WHERE id=?",
    (new_id,),
).fetchone()
print("verify:", row)
print("拼多多话术总数:", cur.execute(
    "SELECT count(*) FROM interview_scripts WHERE company='拼多多'").fetchone()[0])
conn.close()
