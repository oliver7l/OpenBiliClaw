# 项目上下文交接文档：笔记模块 P1 存储与服务层

> 生成时间: 2026-09-07 22:52
> 说明: 新建笔记模块（`src/openbiliclaw/notes/`）P1 阶段 — 存储与服务层、API 路由、CLI 命令

---

## 1. 项目基本信息

| 项目 | 值 |
|------|-----|
| 项目名称 | OpenBiliClaw |
| 当前分支 | 未命名（非 main？未提交） |
| 最近提交 | `b88e306e feat: 实现概念反向索引表 — 跨站知识库搜索与浏览` |
| 许可证 | MIT |
| Python | 3.11+ |
| 包管理器 | uv |

---

## 2. 当前活动

### 已完成：笔记模块 P1 存储与服务层

**背景**：研究了两个参考项目并集成到项目中：
- `references/bili-video2book`（MIT，B站视频→笔记转写管线，可搬代码）
- `references/wandao`（AGPL-3.0，多平台知识库导入导出工具，只借鉴设计思想不抄代码）

完整方案见 `docs/notes-module-proposal.md`。

### 本次新增文件

| 文件 | 说明 |
|------|------|
| `src/openbiliclaw/notes/__init__.py` | 模块入口，导出所有公开类 |
| `src/openbiliclaw/notes/models.py` | Pydantic 数据模型：Note、NoteCreate、NoteUpdate、NoteTask、NoteListParams、NoteStats |
| `src/openbiliclaw/notes/store.py` | SQLite 存储层：notes/note_tasks 建表 + FTS5、CRUD、搜索、统计、任务管理 |
| `src/openbiliclaw/notes/service.py` | 业务逻辑层：NoteService（CRUD、任务管理、已读库导入） |
| `src/openbiliclaw/api/notes_routes.py` | FastAPI 路由：笔记 CRUD、搜索、统计、任务管理、导入端点 |
| `docs/notes-module-proposal.md` | 完整集成方案与笔记模块设计文档 |

### 本次修改文件

| 文件 | 修改内容 |
|------|----------|
| `src/openbiliclaw/api/app.py` | 导入并注册 `register_notes_routes` |
| `src/openbiliclaw/cli.py` | 添加 `note_app` Typer 命令组，包含 `list/get/create/delete/search/stats/import-read-archive/tasks` 命令 |

### 未提交（全部在 Working Tree 中，无所谓先提交哪个）

本次修改的笔记模块相关文件：
- `src/openbiliclaw/notes/`（新建）
- `src/openbiliclaw/api/notes_routes.py`（新建）
- `src/openbiliclaw/api/app.py`（已修改）
- `src/openbiliclaw/cli.py`（已修改）

此外还有大量其他修改（discovery/、llm/、web/、diary/、extension/ 等），与本次笔记模块无关，是已有工作区改动。

---

## 3. 架构决策

### 设计的核心原则

1. **独立存储，追加模式**：`NoteStore` 可复用主 `Database` 实例连接，建表脚本在 `_SCHEMA_SQL` 中独立管理（`CREATE TABLE IF NOT EXISTS`），不侵入主库 `database.py` 的初始化。模式完全对齐 `diary/store.py`。
2. **懒加载初始化**：`NoteService` 在 API 第一次请求时通过 `_get_notes_service()` 创建，对齐 `diary_routes.py` 模式。
3. **FTS5 全文检索**：`notes_fts` 虚拟表 + 3 个触发器同步（insert/delete/update），tokenize 用 `unicode61`。
4. **临时文件策略**：音频转写等中间产物走系统临时目录，管线完成后清理，只有最终笔记正文和清洗转录文本持久化（用户明确要求）。
5. **数据模型**：`notes` 表 + `note_tasks` 表，不进 `pool.db`，不直接读写用户 `notes/` 目录。`metadata` 字段为 JSON 扩展字段，借鉴 wandao 的设计思想。

### 模块关系

```
src/openbiliclaw/notes/
├── __init__.py    → 导出 Note, NoteCreate, NoteService, NoteStore 等
├── models.py      → Pydantic 模型
├── store.py       → NoteStore（SQLite CRUD + FTS）
└── service.py     → NoteService（业务逻辑，含 import_from_read_archive）

api/notes_routes.py  → FastAPI 端点（通过 register_notes_routes 注册）
cli.py              → note 命令组（通过 note_app typer 注册）
```

---

## 4. 关键代码位置

| 用途 | 文件 | 关键行 |
|------|------|--------|
| 数据模型定义 | `src/openbiliclaw/notes/models.py` | 全文件 |
| 建表 SQL + 存储层 | `src/openbiliclaw/notes/store.py` | `_SCHEMA_SQL` 行 20-79, `NoteStore` 类 |
| 业务逻辑 | `src/openbiliclaw/notes/service.py` | `NoteService` 类 + `import_from_read_archive` |
| API 路由注册 | `src/openbiliclaw/api/notes_routes.py` | `register_notes_routes()` |
| API 路由集成点 | `src/openbiliclaw/api/app.py` | 行 854（import）, 行 8126（register） |
| CLI 命令组 | `src/openbiliclaw/cli.py` | 行 110-111（注册）, 行 4220-4389（命令实现） |
| 集成方案文档 | `docs/notes-module-proposal.md` | 全文件 |
| bili-video2book 参考 | `references/bili-video2book/` | 全库 |
| wandao 参考 | `references/wandao/` | 全库 |

---

## 5. 下一步工作（P2-P4）

### P2 视频转笔记（建议优先）

需要从 `references/bili-video2book/`（MIT）搬运：
- `src/core/fetcher.py` → playurl 音频流获取 + 防盗链下载（复用主项目 WBI 签名）
- `src/core/audio_chunker.py` → ffmpeg 无损切片
- `core/transcriber.py` → faster-whisper 本地转录（可选依赖）
- `generator/cleaner.py` → 非破坏性文本清洗
- `generator/prompt_templates.py` → 全套 prompt 模板（对接主项目 `llm_service`）
- `generator/kernel_extractor.py` → 知识元原子抽取
- 整合到 `notes/pipeline.py` + `notes/transcribe/` 子目录

**注意**：搬运 MIT 代码时在文件头保留原版权声明。

### P3 系列聚合与知识层

- `generator/topic_planner.py` → 多集知识块规划
- `generator/block_synthesizer.py` → 跨集聚合
- embedding 入库 + diary 知识图谱打通

### P4 导入导出与画像回流

- 导入导出（wandao 式安全路径、增量检测）
- 画像/推荐回流信号

---

## 6. 参考文档索引

| 文档 | 路径 |
|------|------|
| 笔记模块完整方案 | `docs/notes-module-proposal.md` |
| 项目架构 | `docs/architecture.md` |
| 项目 Spec | `docs/spec.md` |
| 开发主线 | `docs/v0.1-todolist.md` |
| 变更日志 | `docs/changelog.md` |
| Memory 模块文档 | `docs/modules/memory.md` |
| Diary 模块文档 | `docs/modules/diary.md` |
| CLI 命令参考 | `docs/modules/cli.md` |
| 配置参考 | `docs/modules/config.md` |
| 存储模块文档 | `docs/modules/storage.md` |
| 仓库规则 | `AGENTS.md` |

---

## 7. 上下文恢复指引

给下一个 AI 的指令：

1. **先读这份交接文档**，了解笔记模块当前状态。
2. **读 `docs/notes-module-proposal.md`** 了解完整设计全景。
3. **当前状态**：P1 存储与服务层已完成，模块可导入运行，API 路由和 CLI 命令已注册。尚未提交 git。
4. **如果继续 P2**：从 `references/bili-video2book/` 的 `src/core/fetcher.py`、`audio_chunker.py`、`transcriber.py` 和 `src/generator/` 下的 cleaner、prompt_templates、kernel_extractor 搬运代码到 `src/openbiliclaw/notes/transcribe/` 和 `src/openbiliclaw/notes/synthesis/`，注意保留 MIT 版权声明。音频文件走系统临时目录，用完即删。
5. **许可证红线**：`references/wandao/` 是 AGPL-3.0，只看设计不抄代码。
6. **验证命令**：`unset PYTHONHOME && unset PYTHONPATH && .venv/bin/python -c "from openbiliclaw.notes import NoteService; print('ok')"`。
7. 如果有大量未提交的修改与笔记模块无关，注意只提交笔记模块相关文件。

---

## 8. 建议下一会话使用的技能

| 任务类型 | 推荐技能 |
|---------|---------|
| 继续 P2 视频转笔记 | 直接开发（无需特定技能） |
| 测试笔记模块 | `/tdd` - 测试驱动开发 |
| 需求确认 | `/grill-me` - 深入讨论 |