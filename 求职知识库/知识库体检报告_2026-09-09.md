# 求职知识库 · 解析入库体检报告

> 体检时间：2026-09-09 10:06 – 10:55（含 OCR 阶段）
> 数据库：`_系统_知识库引擎/数据/面试资料总库.db`
> 备份：`面试资料总库.db.bak-0909-1009`（350MB，修复前状态）

---

## 一、结论先行

**体检前：库内 2637 万字，其中 52%（1361 万字）是没解析的编码垃圾。**
**体检后：2169 万字，正常可读率从 61.2% 提升到 79.6%+，OCR 又抢救回 128 万字图片/扫描内容。**

数字变化是**真实化**，不是丢失 —— 原来标称"10 万字"的文档，解码后真实内容只有 3741 字；而 OCR 让原本只能看标题的《技术获奖.pdf》《公司内部推荐系统.pptx》等内容首次可读可检索。

| 指标 | 体检前 | 解码+入库后 | +OCR 后 |
|---|---:|---:|---:|
| 有效篇数 | 1834 | 2064 | **2070** |
| 总字数 | 2637 万字 | 2036 万字 | **2169 万字** |
| 正常可读 | 1122 篇 (61.2%) | 1642 篇 (79.6%) | **1658 篇 (80.1%)** |
| QP 编码垃圾 | 188 篇 / 1361 万字 | 13 篇 / 76 万字 | 13 篇 |
| 腾讯文档 JSON 未解析 | 90 篇 / 83 万字 | 38 篇 / 44 万字 | 38 篇 |
| 空壳 (<200字) | 58 篇 | 80 篇※ | **9 个真空壳（已确认无源）** |

※ 空壳增加是因为新入库 209 篇里含 11 篇本身就为空的文件，属正常暴露。

---

## 二、发现的主要问题

### ① Confluence 导出的 MIME + quoted-printable（最严重）

188 篇 / 1361 万字。文件是 Confluence 导出的邮件格式：

```
Subject: Exported From Confluence
Content-Type: multipart/related; boundary="----=_Part_19594_..."
Content-Transfer-Encoding: quoted-printable
<html ...><p>=E4=BA=91=E4=B8=8A=E9=87=8D=E6=8E=92...</p></html>
```

正文被 HTML 标签和 `=E4=B8=AD` 转义双重包裹，**等于完全没解析**。

虚胖验证：《云上重排无量模型开发.doc》标称 10.0 万字 → 解码后真实内容 **3741 字**，缩水 93%。

> 注：`kb_fix_confluence.py` 此前检测为 0，是因为文件路径在用户整理目录期间失效，脚本从未真正跑通过。

### ② 腾讯文档 JSON 未展开

90 篇。content 字段里塞着 JS 对象字面量，内层还有一层 JSON 字符串：

```js
{ content: [ { type: 'text', text: '{"content":"...\\n..."}' } ] }
```

导致《Constitutional AI 调研》《DeepSpeed-Chat 调研》《PPO 调研》等 LLM/RLHF 方向资料全部不可读。

### ③ 220 个文件压根没入库

主要是 `08_解码文本/` 下的 txt（178 个）和 `02_我的笔记/01_程序化广告/` 的 part01-17。
原因：`kb_ingest.py 运行 --限制 N` 按 rel_path 排序扫全库，总是先扫到已入库的文件，未入库的轮不上。

### ④ "网络请求失败"幽灵文档（16 篇）

当年抓取 Confluence 时网络失败，只存了个标题：

```
优化用户长期价值策略（一）\n\n\n 网络请求失败！
曝光量分布约束下的分配策略\n\n\n 网络请求失败！
模拟推荐排序系统\n\n\n 网络请求失败！
```

已从磁盘同名原件抢救 14 篇，但**原件本身也是同样的碎片** —— 这批内容确已丢失，本地无法恢复。

---

## 三、已执行的修复

| # | 修复项 | 结果 |
|---|---|---|
| 1 | Confluence MIME/QP 解码（164 篇） | 1209 万字 → 79 万字真实内容 |
| 2 | 合并多 part 二次解码（11 篇） | 救回《一二级兴趣召回》《各种emb实验》《达人召回梳理》12825字 |
| 3 | 腾讯文档 JSON 解析（59 篇） | 42.1 万字 → 16.1 万字可读 |
| 4 | 定向入库未入库文件（220 个） | 新增 209 篇 / +620 万字 |
| 5 | 抢救网络失败文档（14 篇） | 从原件重提取，内容仍残缺 |
| 6 | 清洗 title 邮件头垃圾（32 条） | 去掉 `· Date: Tue, 29 Mar 2022...` 后缀 |
| 7 | 清理 Confluence 页眉噪声（172 篇） | 去掉 `1024x640 / 72 / Print / 90` |
| 8 | 重建全文索引 | 2064 篇已可检索 |

**新增脚本**：`_系统_知识库引擎/scripts/kb_ingest_new.py`
—— 先算「磁盘有、库里没有」的差集再入库，解决 kb_ingest.py 扫不到未入库文件的问题。
用法：`python3 kb_ingest_new.py 预览` / `执行 [N]`

---

## 四、OCR 阶段成果（10:20-10:55 补充）

**工具：macOS 原生 Vision OCR（中文最佳）+ pymupdf 文字层 + PPTX XML 直提**，三层方案：

| 方案 | 成果 | 字数 |
|---|---:|---:|
| PDF 文字层/OCR（快提+图片 5 篇） | 含《用户访谈周报》《技术获奖.pdf》《经验交流》 | **32 万字** |
| 扫描版全本 OCR（3 本技术书 +1 副本） | Shell脚本攻略 272页 / 统计学习方法(李航) 251页 / 机器学习实战 148页 | **96 万字** |
| PPTX XML+备注 直提 | 13 篇小 PPT（此前提取器 bug 只拿 83B，实际 XML 里有 42 段文字） | +13 篇 |
| PPTX XML+备注+图 OCR | 5 篇大 PPT 含《看点图集-多目标》《公司内部推荐系统》(195字→3397字) | +5 篇 |

**OCR 带来的最大产出（口径级）**：`《技术获奖.pdf》`OCR 后读到技术攻坚奖原文——**"引入 MMoE 多目标模型……BG 第一个在无量3.0 全量上线多目标的业务"**，直接推翻此前"MMoE 检索 0 次"的结论（见 §五.Ⅵ 口径修正）。已同步修正 4 份面试弹药中的相关警告。

## 五、仍未解决（已确认本地无源，可闭眼）

| 类别 | 数量 | 原因 |
|---|---:|---|
| 网络请求失败 | 14 篇 | 原件本身残缺，需重新抓 Confluence（内网可能已不可达） |
| 腾讯文档 JSON | 38 篇 | 结构变体，当前正则未覆盖（如 PPO/DeepSpeed 之外的变体） |
| QP 编码 | 13 篇 / 76 万字 | 解码后中文仍 <150 字，原文档可能为图片/表格 |
| 真空壳 | 9 个 | 耗时监控.pptx(1页0字)、冷启动内容分析.xlsx(空表)、7 个 0KB 解码 txt（抓取时即失败） |
| 技术书籍正文 | 56 篇 | 未 OCR，用户明确"后面再看"，主动搁置 |

---

## 六、复用经验（下次别踩坑）

1. **入库 ≠ 解析**。统计"万字"没有意义，必须做内容结构审计（是否含 `Content-Transfer-Encoding`、`{"content"` 等标记）。
2. **中文占比判据要先压空白**。Confluence 导出文本塞满空行，直接算中文占比会误杀可救文档（我就是靠这个多救回 11 篇）。
3. **不要用 `unicode_escape` 解 JSON 转义**，会把 UTF-8 中文打成 latin-1 乱码（`è°ç妍`）。必须用 `json.loads`。
4. **动数据库前先备份**。本次备份 350MB，修复出错可回滚。
5. **"检索 0 次"的结论不可靠，先确认文档有没有被正确解析**。本轮最大教训：MMoE 在"82 篇原文检索 0 次"的结论来自编码垃圾时代；解码+OCR 后铁证如山（技术攻坚奖 + 答辩 PPT），直接推翻面试口径。**做任何"原文无据"判断前，必须先跑一遍质量审计**。
6. **大 PPT 别先入为主判"图片容器"**：50MB PPT 只有 83B 是提取器 bug；XML 直提（`<a:t>` 标签 + notesSlides 备注）能拿到 195~481 字，media 大的才需 OCR。分层处理：XML 直提 → 快提文字层 → 图片 OCR。
7. **macOS 原生 Vision OCR 中文效果好**，装 `pyobjc-framework-Vision pyobjc-framework-Quartz` 即可用；比 tesseract 中文包强（本机 tesseract 无中文包）。

---

## 七、P0 数据库优化（2026-09-09 午后，同一天追加）

对 `面试资料总库.db` 做了第一档结构优化，**不改表结构、只动索引与运维面**：

| 项 | 优化前 | 优化后 |
|---|---:|---:|
| 库大小 | 334 MB（含 FTS 全文副本 + 碎片） | **123 MB**（VACUUM 后，-63%） |
| 全文索引 | FTS5 普通表，**每次入库全量 rebuild**（45s） | **external content 表 + 三触发器增量同步**（4.4s，-90%） |
| journal | 默认 delete | **WAL** + wal_autocheckpoint=1000 |
| sha 字段 | **路径的 md5（去重形同虚设）** | **内容指纹 md5(content)**，可作为去重主键 |
| 内容级去重 | 无 | **310 组 376 条 duplicate**（jdk/eclipse 配置拷贝、08_解码文本整理残留、简历副本），保留原始库最优位置 |
| 防复活 | duplicate 会被下次入库覆盖回 ok | skip 条件纳入 duplicate + ON CONFLICT CASE 保护 |
| 备份 | 手动 cp | **`VACUUM INTO`** 一致性快照（228MB）；`.gitignore` 补 `*.db.bak*` `*-wal` `*-shm` |
| 库内正文 | 23MB | 2144MB?→ 实为 2314 万字（01_书籍 66 本正文自动入库，含深度学习 78 万字 / PRML 67 万字） |

### 关键文件改动
- `scripts/kb_ingest.py`：FTS 建表改 external content（content='doc', content_rowid='id'）+ 三触发器（doc_fts_ai/ad/au）；`rebuild_fts` 改 delete-all + 重插（仅迁移/修复用，日常入库不再调用）；search 改 `d.id = doc_fts.rowid` join + `AND d.status='ok'`；**sha 改内容指纹**；ON CONFLICT 加 duplicate 防复活 CASE；skip 条件纳入 duplicate。
- `scripts/kb_compact.py`：rebuild 逻辑同步适配。

### 注意
- external content 表的 `COUNT(*)` 会透传到 doc 表（2183 ≠ 索引行数），查真实索引行数需 `MATCH '*'`。
- 剩余 5 本扫描书（黑客与画家/Effective C++/剑指offer/李航第二版/编写高质量代码）+ 耗时监控.pptx 仍 need_ocr，未 OCR。
- 磁盘上仍有重复物理文件（08_解码文本 新旧两处、岗位弹药库简历副本），**待用户整理目录时清理物理文件后，duplicate 状态自然收敛**（已做防复活保护）。
