# 大模型推荐系统调研_1

> 来源: 腾讯文档 file_id=MEmLmoccUJVD
> 提取时间: 2026-09-04


    - 大模型推荐系统调研

    - fine-tune LLM for recommendation
- 基于一个已经预训练好的大模型，使用特定推荐业务数据进行微调（SFT）使其具备更好的推荐能力。（相对直接使用prompts/in-context learning）基于用户的历史交互数据构建训练样本，使用LoRA进行轻量级的快速微调
- TALLRec: An Effective and Efficient Tuning Framework to Align Large Language Model with Recommendation 中科大
- Rec Input - "User Preference: item1,Item4,...,itemn.                      User Unpreference:item2,item3,...,itemn-1.                      Whether the user will enjoy the target : itemn+1Rec Output - "Yes./No." 
- 样本

    - fine-tune LLM for recommendation
- GPT：根据前面的词预测下一个词 VS 推荐：根据用户前面的历史预测用户下一个服务使用itemid，title，content等内容将item转换为semanticIDs（rawdata->embedding->压缩为semanticIDs）使用semanticIDs序列预测下一个物品的ID（生成的ID中，只有2%不在内容库）用于召回阶段
- Recommender Systems with Generative Retrieval（Google）

    - LLM的性能&成本问题
- LLM延迟：秒级(5-30s)推荐系统要求:毫秒（<500ms）大模型的成本：频繁调用会导致较高的成本解决思路 - 缓存，向量数据库，缩小模型：预先使用大模型生成好推荐结果或者向量，并缓存gpt生成的结果线上通过key-value或者向量数据库查找缓存的结果缩减大模型的规模：蒸馏，剪枝，量化...

    - AndesAI

    - AndesAI

