# 关注作者兴趣&兴趣探索boost&搜索承接优化 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/微视账号推荐/关注作者兴趣&兴趣探索boost&搜索承接优化.doc` ｜ 1318字

## 一句话定位
关于通用推荐广告相关的技术资料（《关注作者兴趣&兴趣探索boost&搜索承接优化》），含 12 条关键要点。

## 核心内容摘要
这个得想想 ） 3. 基于用户关注的兴趣画像计算 3.1. 基于作者账号垂直度画像统计计算 根据作者分类垂直度来进行统计（这里需要限制发文数量，纯度以及最高类目视频个数占比满足一定条件） 类比于计算作者账号垂直度，以用户为角度统计关注作者的垂直度画像（按照关注作者垂直度类别数量排序，并计算相应类目的历史播放完整度作为参考指标） 4. 兴趣探索优化 4.1. 基于用户关注画像的兴趣探索 基于用户关注画像的召回通路 基于用户关注画像的boost逻辑 基于用户关注画像的强插逻辑（对于太久之前关注导致之后没出的行为采用强插逻辑进行试探，这个得在uu先计算写入保送召回通路） 4.2. 兴趣探索策略强插改Boost优化 老用户低频兴趣boost（提权幅度依据汤姆森采样得分来计算） 新用户随机探索boost；接入query召回接口（包括query改写，分词等召回方式） query到tag的模型预测，并把

## 关键方法 · 模型 · 指标
- 这个得想想 ） 3. 基于用户关注的兴趣画像计算 3.1. 基于作者账号垂直度画像统计计算 根据作者分类垂直度来进行统计（这里需要限制发文数量，纯度以及最高类目视频个数占比满足一定条件） 类比于计算作者账号垂直度，以用户为角度统计关注作者的垂直度画像（按照关注作者垂直度类别数量排序，并计算相应类目的历史播放完整度作为参考指标） 4. 兴趣探索优化 4.1. 基于用户关注画像的兴趣探索 基于用户关注画像的召回通路 基于用户关注画像的boost逻辑 基于用户关注画像的强插逻辑（对于太久之前关注导致之后没出的行为采用强插逻辑进行试探，这个得在uu先计算写入保送召回通路） 4.2. 兴趣探索策略强插改Boost优化 老用户低频兴趣boost（提权幅度依据汤姆森采样得分来计算） 新用户随机探索boost
- 接入query召回接口（包括query改写，分词等召回方式） query到tag的模型预测，并把结果写入session中，供query到tag的匹配召回 将query的全匹配改成模糊匹配召回，但这样会引起召回准确度的问题 （当前可直接优化） 基于query切词的tag全匹配召回 （当前可直接优化） 之前有关注作者行为，为什么推荐页感知那么低
- 建立基于用户关注作者的兴趣画像计算，并建立召回通路 对于关注作者发文量低或者关注作者的画像推荐太少的问题，可建立关注作者画像的兴趣探索（关注作者垂类进行boost，对于太久之前关注导致之后没出的行为采用强插逻辑进行试探） 基于喜欢作者画像召回相似作者发文视频（当前线上已有，是否可以做boost逻辑，提高关注率
- ） 兴趣探索的频率过低，可能导致用户很难沉淀至画像中
- 搜索承接优化 1. 整体架构及思考 基于tom的case并结合关注作者生态建设引发的思考，分为以下几点
- 搜索行为但是在推荐页没有承接
- 当前采用末尾强插逻辑，只插入一位导致其他可能的兴趣点无法得到有效的曝光
- 搜索承接优化 1024x640 72 Print 90 关注作者兴趣&amp
- 关注时间比较久远，当关注作者发文量低时，推荐页行为无法累积导致无法推出相关内容
- 目前是query到tag的全匹配映射，主要还是因为query到tag的匹配过于粗暴，匹配程度低
- 将当前的低频兴趣探索，随机兴趣探索逻辑下沉至boost，保留搜索，外部画像，热点承接的强插逻辑 &nbsp
- 2. 作者账号垂直度计算（包含达人以及非达人） 2.1. 分类垂直度（一级/二级） 统计作者发文量，不同分类下的发文量，作者发文类目个数 计算个数最多的分类的视频个数占比，计算发文类目的纯度（信息熵） 数据格式（personid，是否达人， 发文数量， 最高类目发文数量，作者发文类目个数， 最高类目，纯度（信息熵），最多类目视频个数占比） 2.2. 标签垂直度（置信的标签、精准兴趣标签） 类似于1.1方式，计算topN的标签，并去除质量类标签以及其他无关兴趣的标签（ 哪些标签算是精准兴趣标签

## 与推荐/广告/数据科学面试的关联
- 召回策略（向量/图/协同过滤/多路）
- 排序模型（Wide&Deep/DIN/ESMM/树模型）与重排
- 用户画像/分层/生命周期

## 与本人项目的关联
通用推荐广告（文中出现相关关键词）

## 价值评估
**高** —— 与本人项目（通用推荐广告）直接关联，且含方法/指标/术语

