# 多目标融合自动调参 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/多目标/多目标融合自动调参.doc` ｜ 4601字

## 一句话定位
关于通用推荐广告相关的技术资料（《多目标融合自动调参》），含 25 条关键要点。

## 核心内容摘要
第二次实验，优化时长，单vv时长上升，但是vv下降，总时长没有显著增长；search result 文件（csv格式，head：alpha,beta,gamma,delta,auc,mean_watch_duration,mean_duration,click_proportion,like_proportion），其中click_proportion，like_proportion是这组参数下的分数排序后top1/3样本的点击和点赞占比, mean_duration,mean_watch_duration 是top1/3的平均视频时长和平均观看时长

## 结构大纲
- kafka消费组：
- 参数搜索：
- 输入：
- 读搜参结果
- base参数各项离线指标
- 计算其他参数和base参数各指标变化情况
- auc 观看时长增加
- 计算点击 点赞 变化
- 按点击量增加阈值过滤
- 计算观看时长和视频时长差值 视频时长太长 vv会掉很多
- 按计算观看时长和视频时长差值过滤
- 按时长or点赞排序
- else:
- 初步实验效果：
- 存在问题：
- 附件下载：

## 关键方法 · 模型 · 指标
- 第二次实验，优化时长，单vv时长上升，但是vv下降，总时长没有显著增长
- search result 文件（csv格式，head：alpha,beta,gamma,delta,auc,mean_watch_duration,mean_duration,click_proportion,like_proportion），其中click_proportion，like_proportion是这组参数下的分数排序后top1/3样本的点击和点赞占比, mean_duration,mean_watch_duration 是top1/3的平均视频时长和平均观看时长
- select best v1（代码中select_best_v1函数）： 最后主要优化点赞，发现整体指标都有所提升
- # 按时长or点赞排序
- # auc 观看时长增加
- 3流join的问题，曝光点击join之后要再等一个窗口再和点赞join，现在延迟比较大，有2个时间窗口的延迟
- 参数选择策略
- 初步实验效果：
- 多目标融合自动调参
- # 计算点击 点赞 变化
- # 按点击量增加阈值过滤
- # base参数各项离线指标
- 以用户粒度进行这种方式的调参
- 多目标融合参数搜索初探.pptx
- # 按计算观看时长和视频时长差值过滤
- # 计算其他参数和base参数各指标变化情况
- # 计算观看时长和视频时长差值 视频时长太长 vv会掉很多
- val isNewUser = args(2).toString #是否为新用户
- 点击：cg_yoorecappgroup_multitask_integrate_streaming_click
- 曝光：cg_yoorecappgroup_multitask_integrate_streaming_exposure
- candi_df = res_df.loc[res_df['auc'] > base_record['auc']].loc[
- -flink任务： com.tencent.sparkx.app.Ranking.MultiTaskIntegrate.streaming.FlinkStream
- (abs(res_df['alpha'] - alpha_base) <= epsilon) & (abs(res_df['beta'] - beta_base) <= epsilon) & (
- abs(res_df['gamma'] - gamma_base) <= epsilon) & (abs(res_df['delta'] - delta_base) <= epsilon)].iloc[0]
- best_record = candi_df.sort_values(by=['mean_watch_duration', 'like_increase'], ascending=[False, False]).iloc[0]

## 与推荐/广告/数据科学面试的关联
- 排序模型（Wide&Deep/DIN/ESMM/树模型）与重排
- 多目标排序（MMoE/ESMM/帕累托）
- 用户画像/分层/生命周期
- AB 实验与评估框架

## 关键术语
AUC、DIN、KS、PLE、WARNING

## 与本人项目的关联
通用推荐广告（文中出现相关关键词）

## 价值评估
**高** —— 与本人项目（通用推荐广告）直接关联，且含方法/指标/术语

