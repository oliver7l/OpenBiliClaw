# DSSM+召回二期 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/腾讯视频/DSSM+召回二期.doc` ｜ 1349字

## 一句话定位
关于通用推荐广告相关的技术资料（《DSSM+召回二期》），含 27 条关键要点。

## 核心内容摘要
1. 使用的DCN网络结构：双塔的一侧为用户特征和上下文特征，通过embedding等处理后，通过一个 deep cross network，得到用户的向量表示；但是，一方面召回深度模型和排序深度模型使用的不同训练框架，不利于维护和进一步优化

## 结构大纲
- 1、召回深度化的进一步探索
- 2、时长目标收益改善
- 1. 在dlrm框架上实现dssm模型，离线对比效果并上线
- 2. 对采样方式进行实验，选择较优的采样方式
- 3. 使用长期收益作为模型的目标
- 2. 增加计算特征：相比上一版本的DSSM，增加了以下特征
- 2.1 lbs省份
- 2.2 lbs城市
- 2.3 手机平台
- 2.4 手机型号
- 2.5 平台分桶
- 2.6 平台策略id
- 2.7 推荐场景
- 2.8 用户对类目的兴趣特征
- 2.9 用户请求是周几
- 2.10 用户请求是当天几点
- 1. 使用dlrm模型后模块的指标提升很明显，但是整体页面无差异

## 关键方法 · 模型 · 指标
- 1. 使用的DCN网络结构：双塔的一侧为用户特征和上下文特征，通过embedding等处理后，通过一个 deep cross network，得到用户的向量表示
- 但是，一方面召回深度模型和排序深度模型使用的不同训练框架，不利于维护和进一步优化
- 使用DCN网络结构 + 增加计算特征 + 优化负样本采样方式 + 修改召回目标为未来三天的累计时长收益
- 去年在精选feeds新样式聚合模块全量了DSSM+youtubeDNN+热门 的多路召回策略，带来了曝光人均时长和曝光人均vv 2.0+%的收益
- 双塔的另外一侧是item特征，通过处理后得到item向量
- 1. 使用dlrm模型后模块的指标提升很明显，但是整体页面无差异
- 另外一方面，还有很多上下文特征没有考虑到，还有进一步的优化提升空间
- 上一版本的深度召回还是以vv收益作为目标，因此模型的目标需要更好的去考虑长期带来的时长收益
- 2.8 用户对类目的兴趣特征
- 1. 在dlrm框架上实现dssm模型，离线对比效果并上线
- 3. 旧版本的DSSM采用实际的曝光作为负样本，或正样本负采样作为负样本
- 目前长视频最主要的目标，是通过提升精选频道的播放时长，从而带来大盘的时长提升
- 第二期DSSM对于负样本进行了深入探索，采用 实际曝光负样本 + 随机负采样 结合的方式，解决训练和线上计算样本空间不完全一致问题
- 4. 修改召回目标：拟和目标修改为未来三天（包括当天）的累计时长收益，并对时长做log转换，转化逻辑为log(duration/a + 1.0)*b + 1.0 (a和b为可调常数)
- 2.7 推荐场景
- DSSM 召回二期
- 2、时长目标收益改善
- 2.6 平台策略id
- 2.9 用户请求是周几
- 1、召回深度化的进一步探索
- 2.10 用户请求是当天几点
- 3. 使用长期收益作为模型的目标
- 最后通过转换后的时长对目标损失函数进行加权
- 2. 对采样方式进行实验，选择较优的采样方式
- 2. 增加计算特征：相比上一版本的DSSM，增加了以下特征
- https://www.ijcai.org/Proceedings/2019/0309.pdf
- 2. 在1基础上使用长期收益，并调整时长转换参数为log(duration/10000 + 1.0)*70 + 1.0，整体页面收益也为正向

## 与推荐/广告/数据科学面试的关联
- 召回策略（向量/图/协同过滤/多路）
- 排序模型（Wide&Deep/DIN/ESMM/树模型）与重排
- 用户画像/分层/生命周期
- 特征工程/embedding
- AB 实验与评估框架

## 关键术语
DIN、DSSM、Embedding

## 与本人项目的关联
通用推荐广告（文中出现相关关键词）

## 价值评估
**高** —— 与本人项目（通用推荐广告）直接关联，且含方法/指标/术语

