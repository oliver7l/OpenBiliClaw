# 基于策略梯度的TopK集合召回推荐算法 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/其他/基于策略梯度的TopK集合召回推荐算法.doc` ｜ 10603字

## 一句话定位
关于微视、QQ看点/图集、百度相关的技术资料（《基于策略梯度的TopK集合召回推荐算法》），含 30 条关键要点。

## 核心内容摘要
当然，我们也可以通过一个集合召回算法，从用户群的历史行为中学习如何召回，从提高集合整体的点击率来提升用户体验；我们根据这个思路调整了奖赏的定义，给下载<=3个app的用户行为同样赋予最高的奖赏，这样我们既满足了精选小众下载的需求，又最大化了有集合下载愿意用户的下载能力，上线后达到了预期的效果，在忽略曝光结构的情况下（游戏、广告、自然量的比列），整体分发系数（即人均下载个数）提升20%

## 结构大纲
- 1. 问题描述
- 2. 相关研究
- 3. 算法描述
- 3.1 策略梯度算法：REINFORCE
- 3.2 Off-Policy
- 3.3 TopK召回
- 3.4 裁剪
- 4. 奖赏的重新定义
- 5. 模型结构与实现
- 6. 未来展望
- 7. 参考
- 举几个例子来说明：
- 参数的更新公式如下：
- 7. Trust Region Policy Optimization.

## 关键方法 · 模型 · 指标
- 当然，我们也可以通过一个集合召回算法，从用户群的历史行为中学习如何召回，从提高集合整体的点击率来提升用户体验
- 我们根据这个思路调整了奖赏的定义，给下载<=3个app的用户行为同样赋予最高的奖赏，这样我们既满足了精选小众下载的需求，又最大化了有集合下载愿意用户的下载能力，上线后达到了预期的效果，在忽略曝光结构的情况下（游戏、广告、自然量的比列），整体分发系数（即人均下载个数）提升20%
- 基于策略梯度的TopK集合召回推荐算法
- 如上图所示，TopK算法位于粗召回之后，会从粗召回的N个app中选择21个app，再经过精排和重排干预后最终展示给用户
- 值优化(value-based)： 通过最大化值来找到最优策略，经典的Q-Learning和Sarsa等算法都是值优化方法
- 以youtube视频推荐为例，视频的下方会推荐多个视频，但如果我们都召回源视频相同类目的视频，可能会引起用户的审美疲劳而使用户反感而降低点击率
- 在推荐系统中，我们关注用户与系统的交互行为，记录推荐的动作（这里即推荐的App）和用户的反馈（点击和下载），并基于这些交互行为改善下一次动作，使用户越来越满意
- 策略优化方法必须等一个episode完整结束，才能获得reward去更新，这种更新方式属于Monte-Carlo Policy Gradient，又叫REINFORCE算法
- 以应用宝必备弹窗推广“微视 app”为例，我们会在固定槽位帮助推广“微视app”，但不同的实验算法下“微视”的cvr差距很大，这也间接的说明了集合的推荐结果对于个体效果有很大的影响
- 谷歌公司在18年12月发表论文《Top-K Off-Policy Correction for a REINFORCE Recommender System》，提出的一种新的策略梯度的召回算法，用于一次性召回K个item，并在youtube视频推荐上取得了不错的效果
- 从优化方法角度，强化学习可以分为策略优化和值优化两种
- 本文将以应用宝新机必备弹窗场景的实践为例，详细描述TopK集合召回推荐算法
- 而策略梯度，其实就是策略优化方法中直接求策略的梯度来直接优化策略的一种方法
- 如果按照pCVR排序去展示，很可能达不到很好的效果，因为用户不太可能同时下载多个功能相似的app
- 最终从候选集中一次性选取Top21个app作为必备弹窗的集合召回的结果，再经过排序层后展示给用户
- 在应用宝新机必备弹窗业务中，对于判定为新机的用户，在第一次启动应用宝时会一次推荐最多21个App（详见右图）
- Actor-Critic： 结合了值优化和策略优化，critic网络预估value，actor网络预估action
- 另外，未来会尝试在整个推荐交互场景上加入critic网络来拟合用户行为的长期奖赏，通过更好的刻画和服务用户来提升场景的收益
- 业界通常的做法是：先召回候选items，然后对每个的<user, item>进行打分并按照pCVR对item排序，最后经过重排层展示给user
- 感谢@kelvincai，@nianhuaxie和推荐算法组同学的意见建议，以及@zemianxu，@lyleliao等架构组同学提供的工程侧的指导协助， 有不足的地方请大家指正
- 我们将模型要优化的策略称为目标策略 (mathjax-inline(\pi_\theta)mathjax-inline) ，把实际产生行为的策略成为行为策略 (mathjax-inline(\beta_\theta)mathjax-inline)
- 策略 (mathjax-inline(\pi_\theta(s,a))mathjax-inline) 的优化方向是最大化长期奖赏，长期奖赏可以用很多种方法表示，如start value、average value等，这里我们使用average reward per time-step来表示，则长期奖赏的期望表示如下：
- 下面展示了整个场景的算法流程图
- 采用REINFORCE策略梯度算法
- 3.1 策略梯度算法：REINFORCE
- 但在实验组中，Taptap的曝光量却排在了10名以外
- 在对照组中，”Taptap“获得了最多的曝光和最高的后验cvr
- 在推荐系统中，我们经常面临一次性为user展示多个item的场景
- 于是我们拆分实验组和对照组的曝光app来看，发现一个很有趣的问题：
- 在模型选择上，我们尝试过WND结构、DIN结构、MLP结构，效果差异不大

## 与推荐/广告/数据科学面试的关联
- 召回策略（向量/图/协同过滤/多路）
- 排序模型（Wide&Deep/DIN/ESMM/树模型）与重排
- 用户画像/分层/生命周期
- 特征工程/embedding
- AB 实验与评估框架

## 关键术语
CVR、DIEN、DIN、KS、MDP、REINFORCE、RL、SMiRL

## 与本人项目的关联
微视、QQ看点/图集、百度、通用推荐广告（文中出现相关关键词）

## 价值评估
**高** —— 与本人项目（微视、QQ看点/图集、百度）直接关联，且含方法/指标/术语

