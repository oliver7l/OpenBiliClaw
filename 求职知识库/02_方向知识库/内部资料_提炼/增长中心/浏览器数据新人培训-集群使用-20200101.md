# 浏览器数据新人培训-集群使用-20200101 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/增长中心/浏览器数据新人培训-集群使用-20200101.pdf` ｜ 12029字

## 一句话定位
关于QQ看点/图集相关的技术资料（《浏览器数据新人培训-集群使用-20200101》），含 30 条关键要点。

## 核心内容摘要
更推荐点击【通过页面编写字段】来配置；类型：大多数时候都是选 ETL 表，对于算法和推荐来说，也许会选 sample 表

## 结构大纲
- 方法是：
- 下面逐一说明每个选项的含义：
- 表描述：请用一段话描述表大致是做什么的。
- 保存周期：选择一个合适的保存天数，数据存储有成本，并不是越长越好。
- 1. 必须清晰明确地用 WHERE 限定分区。
- 3. 最外层的 SELECT 必须一行一个字段，字段数量与目标表对齐。
- 5. 不同调度类型依赖上一周期：比较少用，含义跟依赖全部是差不多。
- 还是单独强调一下：
- 1. 优先级，要设置合理的优先级，跟 KPI 相关的一点是紧急。
- 3. 告警，重要的任务都要有告警，不然每天都得手动查看是否正常。
- 列表：
- 1. varchar 的长度要修改为合适的长度，否则超出有效范围，建表会报错。
- 4. 如上一条，如果需要自增变量，则需要添加 id 字段为自增变量。
- 5. 作为结果表，字段都不应该为空，可以声明字段不允许空值。

## 关键方法 · 模型 · 指标
- 更推荐点击【通过页面编写字段】来配置
- 类型：大多数时候都是选 ETL 表，对于算法和推荐来说，也许会选 sample 表
- 与浏览器相关的信息是，统计分析默认用【深圳 new- 汕尾 1 系列】集群，而算法推荐默认用
- SELECT * FROM table_name WHERE ds LIKE '20191210%'
- SELECT a,b,c FROM table_b WHERE ds LIKE '20191212%'
- SELECT a,b,c FROM mtt.table_a WHERE ds LIKE '20191212%'
- SELECT a,b,c FROM u_wsd.table_a WHERE ds LIKE '20191212%'
- 用户编辑和修改
- 界面说明直接看图
- 定时器截图如下：
- 由于信息都明白如话，就不展示截图了
- 接下来需要配置表的具体信息，如下图：
- 下图简要介绍了 HIVE-DB 组件的
- a- 全量表，例如存储有史以来所有用户的
- 统计结果选 m ， m 的表里是不带有用户 id 的
- 建表语句自己需要修改，如上图，需要修改成下面的样子：
- r- 存放配置信息，翻译信息，信息源不是终端用户，而是
- 是在后面，继续编辑自定义的表名称，推荐将业务的英文包含进去
- 对我们来说，推荐使用 LIST 分区，因为 LIST 分区
- 下面这个图是给有权限的管理员们看的，避免他们不会继续扩散权限
- 每次改动都是涉及整个 table_name_20191212 的
- 主要是要场景是，累积全量用户表时，必须依序进行累积，避免日期错乱
- crontab 决定了循环的判断方式，详细的知识需要自己 Google
- 作业名称一般是系统生成，状态启动代表工作中，停止代表整个后续的任务都停止
- FROM table_name_a WHERE ds = 20191212
- 调度参数按默认值运行，会造成巨大的安全隐患，请一定按下图的讲解，逐一思考参数
- 注意事项和要点都在图中，需要数目的是，每一个项目都是极其重要的，都需要认真对待
- 此外，计算资源通过队列进行分配管理，而且一般会多分配，以便实现资源错配，提升使用率
- mttstatads.mdb.mig:18017-- 存放广告数据的，但使用率不高
- 表信息，界面不再截图展示，作为一个认真负责的人，要认真配置字段名，字段类型和字段说明

## 与推荐/广告/数据科学面试的关联
- 用户画像/分层/生命周期
- 留存/时长/活跃 指标与因果

## 关键术语
AND、AS、BY、COMMENT、CPU、CREATE、DAU、DBServer、DFORMAT、ENGINE、ETL、EXISTS、FORMAT、FROM、GBK、HIVE、HVIE、IF、IN、INPUTFORMAT、INSERT、INSTER、KEY、KPI、LASTDAY、LIKE、LIST、MDB、NOT、NULL

## 与本人项目的关联
QQ看点/图集（文中出现相关关键词）

## 价值评估
**高** —— 与本人项目（QQ看点/图集）直接关联，且含方法/指标/术语

