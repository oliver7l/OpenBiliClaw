# 整体梳理 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/看点搜索/李显鑫/整体梳理.doc` ｜ 2928字

## 一句话定位
关于通用推荐广告相关的技术资料（《整体梳理》），含 29 条关键要点。

## 核心内容摘要
模型打分获取模块 （利用资源id获取打分，获取全部模型打分如ctr、ltr、rele、cvr等供综合排序使用，获取指定统计行为特征供后续排序策略使用）；阿拉丁点调排序 （阿拉丁资源和打底资源根据点击模型进行混排，混排结束后打底资源恢复原本相对顺序）

## 结构大纲
- 相关资料：
- 一、主要流程
- 1、定义通信接口
- 2、初始化主服务
- 3、初始化各模块
- 4、实现各接口
- 5、接口内实现应答逻辑
- 二、结果页综合排序处理链
- 代码文件： WSResSort.jce
- 代码文件： QBWSSortPolicyServer.cpp
- 定义应用：QBWSSortPolicyServer g_app；
- 应答接口注册：addServant<WSResSortImp>
- 用户染色接口：dyeUserManage
- 管理命令注册：TAF_ADD_ADMIN_CMD
- 示例代码文件： WSResSortImp.cpp
- dealDye：用户染色
- printReq：日记记录request
- getXXProcessor：获取处理指针
- doProcess：处理request进行各类排序

## 关键方法 · 模型 · 指标
- 模型打分获取模块 （利用资源id获取打分，获取全部模型打分如ctr、ltr、rele、cvr等供综合排序使用，获取指定统计行为特征供后续排序策略使用）
- 阿拉丁点调排序 （阿拉丁资源和打底资源根据点击模型进行混排，混排结束后打底资源恢复原本相对顺序）
- 排序策略服务串讲.pptx
- 相关保护模块（ 对相关打分高于阈值的语义召回/IR找回资源 保护到全核心词匹配队列）
- 将资源基本属性从输入结构赋值到排序服务结构中，进行 初始化排序 （拿到上游比如召回端的资源）
- 对 ctr打分进行干预 （领域意图指定资源、资源意图指定资源、指定账号、指定aid、指定资源类型、评论数）
- 处理上游 传下来的信息（评论数、停留时长、账号名、摘要、动态摘要、资源tag、bw打分、标题实体信息、文章来源、召回词追踪信息等）
- 计算q下资源各个维度的分值（是否被整体后置、后置队列、品类优先队列、匹配队列、时效队列、tag队列、账号队列、资源质量队列、综合队列打分）用在QueuePk的内部排序中
- 二、结果页综合排序处理链
- dealDye：用户染色
- 根据 相关性模型打分 计算分档
- CvrFilterProcess
- CtrFilterProcess
- AladdinSortProcess
- 用户染色接口：dyeUserManage
- 用以 选取模块 组装结果页综合排序处理流程
- 基于 cvr 打分对资源进行 过滤/跨队列后置
- 基于 ctr 打分对资源进行 过滤/跨队列后置
- AladdinSeparationProcess
- doProcess：处理request进行各类排序
- AladdinClickAdjustProcess
- 资源捞回 （当被过滤资源太多时按照 相关性模型打分 进行资源捞回）
- 各个文件夹分别处理对应逻辑，包括干预策略，DBCache、ModelProxyPtr等
- 示例代码文件： QBWSResSortConf.cpp（结果页 综合排序处理链 配置模块）
- 获取特征 （远程调用特征代理，内含一些统计特征等，或者直接从上游获取，本身自带了一些特征）
- 各打底资源分别进行资源内部排序后进行资源间PK排序（PK依据： 综合队列打分>xxxx ）
- 阿拉丁点调干预 （根据配置指定意图下对指定资源类型进行topN保护/按热点的时效性强度信号，把小新闻卡或大事件卡进行topN保护）
- query转发模块 （采样query转发到QBWSQuerySampleServer服务，随机采样三端query，供时效性理想体验评估/稳定性监控使用）
- 按 队列排序取topK资源 （资源内排序+资源间PK混排，PK依据： 综合队列打分>粗排打分 ），其中资源内排序的逻辑是核心词命中>相关性分档>时效性分档后计算得分

## 与推荐/广告/数据科学面试的关联
- 召回策略（向量/图/协同过滤/多路）
- 排序模型（Wide&Deep/DIN/ESMM/树模型）与重排
- CTR/CVR 预估与校准
- 用户画像/分层/生命周期
- 特征工程/embedding

## 关键术语
CTR、CVR、DBCache、DIN、PLE、QBWSResSortConf、QBWSSortPolicyServer、WSResSort、WSResSortImp

## 与本人项目的关联
通用推荐广告（文中出现相关关键词）

## 价值评估
**高** —— 与本人项目（通用推荐广告）直接关联，且含方法/指标/术语

