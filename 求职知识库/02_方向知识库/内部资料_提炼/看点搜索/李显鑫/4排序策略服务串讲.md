# 4排序策略服务串讲 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/看点搜索/李显鑫/4排序策略服务串讲.pptx` ｜ 13248字

## 一句话定位
关于中信、通用推荐广告相关的技术资料（《4排序策略服务串讲》），含 30 条关键要点。

## 核心内容摘要
1、获取rele|cvr|ctr实验参数；2.构造模型请求，请求ctr|rele|cvr

## 结构大纲
- 一、主要流程
- 二、DBCache操作
- 三、ModelProxyRpc
- 四、其他（strategylevel & queuelevel）
- 五、策略细节
- 二、通过m_mysql.queryRecord调用数据库
- 一、直接搜寻DBCache类中成员变量（初始化时已加载）
- 举例：在接口策略中调用DataProcess
- 四、其他
- 最后附上代码层级：
- 四、策略细节
- 包含流程：
- 1.用户染色
- 3.构造requestContextPtr
- 4.获取处理pipeline
- 5.流程处理
- 1.QSugQueryInfo stSug的读入
- 2.构造RequestContextPtr
- 5.返回RequestContextPtr
- 1.获取处理pipeline指针
- 3.prt->doProcess
- 1.初始化排序结果
- 2.处理上游传下来的结果
- 3.获取队列打分
- 4.按队列排序，取tok item

## 关键方法 · 模型 · 指标
- 1、获取rele|cvr|ctr实验参数
- 2.构造模型请求，请求ctr|rele|cvr
- 2. 根据队列及加权ctr进行打分，打分公式如最下图，之后根据分值完成排序
- 2、调用模型代理，请求ctr|rele|cvr，然后将相应分数写入资源字段中
- Cvr和wdl分数传给ctr,同样构造模型代理请求，交给fireCtrModel异步调用，回调handleCtrFuture.
- 打底资源，没有大卡标志，且已有资源满足页面tok，则将资源的iSortIdx(最终位置)置-1，dCtr(ctr模型返回)置0，dWeightCtr(加权Ctr)置0，dCvr(cvr分数),drele(相关性分数)置0，不参与模型打分
- 排序策略服务
- 排序层实验记录
- 召回层实验记录
- 图片综合排序流程
- 3.图片队列打分排序
- 2、根据队列和Ctr打分进行排序
- 与具体应用排序策略逻辑相关，具体详见各策略类
- 4.请求调用结束后单向调用wdl模型积累特征
- 这个接口ctr|rele|cvr调用一次，没有二次操作
- 6,setCommentSupportCtr对指定评论数进行加权，加权方式如图
- 从传过来的请求当中提取资源，初始化排序信息，重点是将dCtr设置为资源的回答数
- 5.在handleFeatureModel中采用函数指针的方式设置从模型代理请求回来的相关性分数/wdl分数/cvr分数
- 1.initWeightCtr 设置item的dWeightCtr为其dtr,即先设置其加权ctr为其模型返回的ctr分数
- 4.获取topKPageSize,对排序超过topKPageSize的打底资源且没有大卡标志，置iSortIdx为-1，不进后续的模型打分
- 2. setWeightCtr先从取意图信息完成领域意图和资源意图的加权，最后利用公式pow（(1+权重)，权重因子）对dWeightCtr进行意图加权
- 4.releaseTestInfo函数用于记录排序层召回层的信息，记录入RequestContextPtr->reqPtr->testInfo中，并打印日志
- dealDye用户染色，printReq日志记录request,buildRequestContextPtr获取请求上下文，getXXXProcessor()获取处理指针，doProcess处理request进行各类排序
- 3.获取item的iCommon(评论)，sDesc(资源原描述),iDuration(总阅读时长)，sAcount(账号)，sAbstractDesc(动态摘要)，sIconUrl(展示Icon的url),iAccountLevel(账号等级)，vecTag(资源tag),如果是小说资源，获取bHaveCopyright(是否正版)，dBWScore(bw打分)，qSugInfo(qbq信息)，vecTitleEntityInfo(标题实体信息)，bIsTitleMultiplePEREntity（是否包含多实体），bIsCloseRelations（标题任务是否关系密切）
- 五、策略细节
- 四、策略细节
- 1.用户染色
- Thanks
- 8.Cvr过滤
- 9.Ctr过滤

## 与推荐/广告/数据科学面试的关联
- 召回策略（向量/图/协同过滤/多路）
- 排序模型（Wide&Deep/DIN/ESMM/树模型）与重排
- CTR/CVR 预估与校准
- 用户画像/分层/生命周期
- 特征工程/embedding
- AB 实验与评估框架

## 关键术语
CTR、CVR、DBCache、DIN、GQBWSSortConf、KS、PLE、QBWSSortPolicyServer、QQKD、QSugQueryInfo、WSResSort、WSResSortImp

## 与本人项目的关联
中信、通用推荐广告（文中出现相关关键词）

## 价值评估
**高** —— 与本人项目（中信、通用推荐广告）直接关联，且含方法/指标/术语

