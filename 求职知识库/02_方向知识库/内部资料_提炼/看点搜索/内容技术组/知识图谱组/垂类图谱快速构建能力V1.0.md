# 垂类图谱快速构建能力V1.0 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/看点搜索/内容技术组/知识图谱组/垂类图谱快速构建能力V1.0.doc` ｜ 1839字

## 一句话定位
关于QQ看点/图集相关的业务/项目资料（《垂类图谱快速构建能力V1.0》），含 19 条关键要点。

## 核心内容摘要
图谱可用性：构建得到垂类图谱准确率、覆盖率与通用图谱一致；接入数据之后，首先按数据源进行领域映射，一个数据源可以用于多个垂类图谱数据，之后各个垂类的数据相互独立的存放在同Hive表的不同分区里进行单独构建，但是分类、对齐、清洗相关的计算逻辑是共用的，值得注意的是，融合阶段是对应分区独立全量融合，不会与其他垂类的数据进行数据交互

## 结构大纲
- 1.背景
- 2.功能
- 图谱可用性：构建得到垂类图谱准确率、覆盖率与通用图谱一致
- 构建迅捷化：构建垂类图谱操作简单， 且整个构建流程耗时不超过2hour
- 业务独立性：支持同时对多个垂类图谱进行批量构建，且图谱之间保持相互独立
- 流程通用性：不同的领域可以复用同一套流程
- 代码复用性：复用全量构建计算逻辑，开发量少
- 2.使用方法
- 3.架构
- 4.计算
- 5.代码
- 6.TODO

## 关键方法 · 模型 · 指标
- 图谱可用性：构建得到垂类图谱准确率、覆盖率与通用图谱一致
- 接入数据之后，首先按数据源进行领域映射，一个数据源可以用于多个垂类图谱数据，之后各个垂类的数据相互独立的存放在同Hive表的不同分区里进行单独构建，但是分类、对齐、清洗相关的计算逻辑是共用的，值得注意的是，融合阶段是对应分区独立全量融合，不会与其他垂类的数据进行数据交互
- 2.使用方法
- 垂类图谱关系建设
- 垂类图谱实体出库
- 对接入垂类数据进行分类对齐
- 垂类图谱快速构建能力V1.0
- 垂类图谱实体出入度与得分计算
- step 4 .最后实体分区即不同垂类图谱数据
- 知识图谱分为百科知识图谱（全领域）和领域知识图谱
- 最后融合建边后的对应分区就是我们需要的垂类图谱数据
- 构建迅捷化：构建垂类图谱操作简单， 且整个构建流程耗时不超过2hour
- 业务独立性：支持同时对多个垂类图谱进行批量构建，且图谱之间保持相互独立
- step 1. 新建垂类图谱定义，并配置数据源到垂类多对一映射，以及垂类数据导入方式
- 基于以上，在现有全领域图谱构建流程的基础上，看点知识图谱实现了一种垂类领域知识图谱的通用解决方案，满足：
- step 2. 由于垂类与数据源绑定，所以可以在运营平台上配置对应数据源的处理规则（分类、清洗、对齐等），若未配置则默认继承通用构建规则
- 如上图所示，可以看到现在垂类图谱支持三种类型的数据接入方式：通过DB导入、通过Hive导入、以及更自由便捷的通过自定义SQL圈定数据导入
- 当前图谱构建流程输出为全领域通用知识图谱，包含了人物、影视、体育等领域，各个领域的数据各异、处理逻辑不一、数据量巨大，但是都是通过同一套流程进行处理，构建流程复杂，且耗费时间长
- 在日常使用中，很多业务只希望能够快速准确产出对应领域的图谱数据，但现看点通用图谱无法在构建流程中对领域进行拆分实现，所以需要实现一种垂类图谱的快速构建能力，能够接入较小且单一的领域数据，经过相对简单的构建流程，快速迅捷的构建面向特殊领域的图谱

## 与推荐/广告/数据科学面试的关联
- 无明显领域关键词，可能为通用业务/数据记录

## 关键术语
TODO

## 与本人项目的关联
QQ看点/图集（文中出现相关关键词）

## 价值评估
**高** —— 与本人项目（QQ看点/图集）直接关联，且含方法/指标/术语

