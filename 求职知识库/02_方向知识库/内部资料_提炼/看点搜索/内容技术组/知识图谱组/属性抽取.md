# 属性抽取 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/看点搜索/内容技术组/知识图谱组/属性抽取.doc` ｜ 4956字

## 一句话定位
关于QQ看点/图集、百度、通用推荐广告相关的技术资料（《属性抽取》），含 30 条关键要点。

## 核心内容摘要
使用实体抽取模块识别出subject的mention，走实体消歧系统，找到图谱中匹配上的subject_id，拿到该subject_id所属的领域的schema，结合抽取策略能覆盖的属性范围，找到property；准确率一般，需要做基于业务场景的后验过滤，但召回能够有较大提升

## 结构大纲
- 1. 背景
- 1.1. 现状
- 1.1.1. 全库属性覆盖率
- 1.1.2. 重点领域属性覆盖率
- 1.2. 数据源
- 1.3. 业界方案
- 2. 目标
- 3. 属性抽取能力架构
- 3.1. 系统自动识别Subject
- 3.2. 属性抽取策略
- 4. 规划
- 5. 未来工作
- 计算公式：
- 3.80%
- 重点领域： 根据图谱当前支撑的线上业务，圈定的具有独立schema的热门领域。
- 17.85%
- 2.2%
- 37.53%
- 8.9%
- 6.22E-2%
- 40.60%
- 31.20%
- 56.45%
- 22.61%
- 14.40%

## 关键方法 · 模型 · 指标
- 使用实体抽取模块识别出subject的mention，走实体消歧系统，找到图谱中匹配上的subject_id，拿到该subject_id所属的领域的schema，结合抽取策略能覆盖的属性范围，找到property
- 准确率一般，需要做基于业务场景的后验过滤，但召回能够有较大提升
- 百度、小米也基于biLSTM+crf、DeepDive等做了基于语义模型的尝试，对bootstrapping[1]的召回都有一定的补充
- 在用户未提供目标实体集的场景，需要系统自主识别出subject的mention和领域，结合schema和抽取策略能覆盖的属性范围走下游模块
- 属性抽取召回率85%
- 召回能够有较大提升
- 抽取准确率较高，但召回不足
- 召回能够有较大提升，尤其是多值属性
- 使用 show平台定期监控图谱指标
- 首先基于百科文本，完成系统构建及抽取效果评估
- [4] KM《搜索query意图识别综述及模板挖掘方法简介》
- 属性抽取能力构建流程如上图所示，其主要包括离线模型构建部分以及在线服务部分
- [2] Schema约束的知识抽取系统架构（“信息抽取”任务冠军队伍报告）.pdf
- 在线流程重点是根据用户是否提供subject走不同的抽取策略、抽取结果的后处理以及入库准备
- 百度、小米都搭建了基于bootstraping[1]的实体属性抽取系统，并且都有较好的精确率
- 因此构建了图谱实体属性抽取系统，进一步挖掘文本中的SPO tuple，补充实体信息，并将属性抽取能力进行沉淀
- 使用实体抽取模块识别出subject的mention并进行粗分类到大领域（e.g. 分到顶级大领域），在通过遍历大领域下的所有小领域的schema，结合抽取策略能覆盖的属性范围，找到property
- 属性抽取精确率95%
- 属性抽取系统一期（覆盖率<80%）覆盖重点领域高优属性包括：
- 抽取精确率较高
- 基于语义模型抽取
- 图谱属性现状摸底
- 实体抽取+属性分类
- 属性抽取系统服务化
- 3. 属性抽取能力架构
- 3.2. 属性抽取策略
- 属性抽取系统可外部调用
- 召回不足，尤其是多值属性
- 对模版抽取的召回进行补充
- 对多个实体属性对分类，计算量大

## 与推荐/广告/数据科学面试的关联
- 召回策略（向量/图/协同过滤/多路）
- 用户画像/分层/生命周期

## 关键术语
BERT、KM、LSTM、PLE、SKE

## 与本人项目的关联
QQ看点/图集、百度、通用推荐广告（文中出现相关关键词）

## 价值评估
**高** —— 与本人项目（QQ看点/图集、百度、通用推荐广告）直接关联，且含方法/指标/术语

