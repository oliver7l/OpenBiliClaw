# 视频内容质量理解 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/看点搜索/内容专项/视频内容质量理解.doc` ｜ 4620字

## 一句话定位
关于QQ看点/图集、中信、通用推荐广告相关的技术资料（《视频内容质量理解》），含 30 条关键要点。

## 核心内容摘要
整体的多模态视频理解模型不仅可以输出视频embedding，同时还可以用于相似视频召回(Video2Video)、跨模态检索(Query2Video)以及图搜视频(Pic2Video)等检索任务，也可用于视频的分类、tagging以及优质视频识别；去掉视频阿拉丁的30%的覆盖率漏斗，观察实验组、对照组都有人搜且实验组曝光的4宫格的query上的数据，点击指标略微提升，视频质量分的引入对前4视频覆盖基本没有影响

## 结构大纲
- 分布：按低质分档，尽量和图文质量分的分布打平
- 策略：与图文相同，卡内1分过滤，打底1分后置
- 结论：视频质量分的引入对top 4视频覆盖和点击指标基本没有影响。
- 附录：内容中心视频质量字段摸底
- 1. 视频多模态embedding简介
- 2. 基于视频多模态embedding特征的排序优化
- 初始队列：A={a1, a2, a3, ..., a30}
- 排序算法：
- 经验值K选择：K=5

## 关键方法 · 模型 · 指标
- 整体的多模态视频理解模型不仅可以输出视频embedding，同时还可以用于相似视频召回(Video2Video)、跨模态检索(Query2Video)以及图搜视频(Pic2Video)等检索任务，也可用于视频的分类、tagging以及优质视频识别
- 去掉视频阿拉丁的30%的覆盖率漏斗，观察实验组、对照组都有人搜且实验组曝光的4宫格的query上的数据，点击指标略微提升，视频质量分的引入对前4视频覆盖基本没有影响
- 2. 基于视频多模态embedding特征的排序优化
- 基于视频embedding聚类重排的实验结果gsb评估中（多类视频embedding横向比较性能）
- 基于视频embedding特征，目前调研了两套方案，1）计算Q召回队列离群分以及与高分发视频的相似度为上层排序提供特征
- 根据视频embedding的相似度选取中心点视频，根据点击（ctr，click）指标选取视频，将选取的视频作为基准，计算其他视频与基准视频的相似度
- 视频embedding离群分以及与高分发视频的相似度分对排序效果的离线评估结果
- 如图一所示，在视频帧间融合上我们采用了业界已验证的NetVlad统计聚类的方法，在整体模型训练方面我们选用了视频二级标签分类任务
- 基于视频embedding的结果集合聚类
- Q召回队列中视频embedding相似度计算
- 2）基于视频embedding做结果集合的聚类，为高分发视频所在类簇结果提权（相关性同档）
- embedding距离紧密度
- 1. 视频多模态embedding简介
- 视频多模态embedding以及搜索应用
- ● 基于视频embedding计算结果集合的相似分布
- 结论：视频质量分的引入对top 4视频覆盖和点击指标基本没有影响
- 从一期GSB中选取了34个query，并获取对应的精排排序拉链和资源对应的ctr等指标
- 详细情况：结果页大盘整体指标：有点率较对照组+0.16%, CTR较对照组+0.10%
- 视频质量分LTR实验的UBS评估结果：top 4 影响面约为20%，看点端+QB端 G：S：B=35:54:15，正收益显著
- #####聚类排序
- 【小流量ABT指标】
- #####多特征视频聚类
- 各特征字段的准召评估（标注）
- 最终，即可得到聚类分桶的排序
- 策略：与图文相同，卡内1分过滤，打底1分后置
- 聚类排序基于各个聚类与K代表之间的Hausdorff距离计算，
- qeh_mismatch_cover_title 封面图和标题不匹配
- ● 图片特征抽取：EfficientNet、BN-Inception
- qeh_mismatch_cover_title =2 封面图和标题不匹配
- 不同于图文等单个模态媒体内容，精准有效的视频理解需要融合多个模态的特征信息

## 与推荐/广告/数据科学面试的关联
- 召回策略（向量/图/协同过滤/多路）
- 排序模型（Wide&Deep/DIN/ESMM/树模型）与重排
- CTR/CVR 预估与校准
- 特征工程/embedding
- AB 实验与评估框架
- 转化漏斗与渠道效能

## 关键术语
BERT、BN、CNN、CTR、DIN、Embedding、LSTM、NCuts、PLE、TRN、TSN、VGGish、Word2Vec

## 与本人项目的关联
QQ看点/图集、中信、通用推荐广告（文中出现相关关键词）

## 价值评估
**高** —— 与本人项目（QQ看点/图集、中信、通用推荐广告）直接关联，且含方法/指标/术语

