# 202006策略梳理 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/看点搜索/搜索商业化/202006策略梳理.doc` ｜ 3636字

## 一句话定位
关于百度、通用推荐广告相关的技术资料（《202006策略梳理》），含 30 条关键要点。

## 核心内容摘要
策略：分不同场景的预估模型（预估数据（来源，更新频率，样本构建），预估特征（哪些几个大类，各类主要的特征），训练效果，有哪些指标数据可评估）；query向量计算（模型，训练数据来源，有哪些指标数据可评估）

## 结构大纲
- 基础检索组提供的sug：——todo：生成方式/评估指标
- 补全比例：直达请求query 64%有召回
- 请求内容：
- 主要内容：发送分词后的“用户原始query”和“补全sug”
- 字段及含义： QBBusinessRewriteObj.jce#L52
- 接收内容：
- 字段及含义： QBBusinessRewriteObj.jce#L62
- 预训练：
- 语料：
- 主题分类：qb浏览器资讯title分类数据
- 微调：
- 代码：
- tf训练： birnn_maskedlm
- taf中对外调用服务： QBBusinessRewriteServer
- 来源：直投广告top1000广告+复用广告已在直达区全量广告的购买词
- 示例：
- 请求命中的比例： t_rd_search_ug_ex_ads_detail
- 排序：不同广告库、不同计费方式之间的排序逻辑（不同广告库的胜出率）
- 需要区分直投和复用的逻辑：
- AMS答复：目前逻辑没有区别。定向触发上有区别，复用因为有定向限制要求更严格
- 需要对比搜狗搜索的广告：
- 预估数据来源：搜索直达
- 更新频率：目前基于天级别，后面可能会迁移到小时级别。
- 预估特征：
- 工程：更新频率，稳定性

## 关键方法 · 模型 · 指标
- 策略：分不同场景的预估模型（预估数据（来源，更新频率，样本构建），预估特征（哪些几个大类，各类主要的特征），训练效果，有哪些指标数据可评估）
- query向量计算（模型，训练数据来源，有哪些指标数据可评估）
- 向量召回bidword（bidword集合（来源、更新机制等），bidword截断逻辑（考虑哪些维度的信息），还有哪些指标数据可评估）
- 匹配方式，不同query的使用方式，触发漏斗（各类过滤），相关性计算，预估逻辑，计价逻辑
- 商业和内容侧的混排逻辑（排序公式，点击率模型的训练逻辑，语料、效果等）——todo：待了解
- 相关性截断：模型计算（参与计算的字段是哪些，训练数据来源，模型的准召，badcase的rootcause，有哪些指标数据可评估）
- 主要内容：改写后得到的召回词和类型（召回词-目前使用广告title替代，类型-用于指明召回方式，目前全量的是faiss召回，正在实验的有字召回）
- 搜狗下发：词表命中逻辑（哪些query会参与词表匹配，匹配逻辑是什么，请求命中的比例，有哪些指标可评估） GetDataFromSogou，原始query和联想词都会去和词表去匹配，精确匹配
- 目前只有直达的CTR，CVR场景
- 基础检索组提供的sug：——todo：生成方式/评估指标
- 排序：不同广告库、不同计费方式之间的排序逻辑（不同广告库的胜出率）
- 广告库规模，不同query的使用方式，广告预估，广告报价，广告样式
- SUG（如何补充sug，训练数据来源，SUG比例，还有哪些指标数据可评估）
- query补全（如何补全，训练数据来源，补全比例，还有哪些指标数据可评估）
- 改写cache，（cache退场机制，cache覆盖比例，有哪些指标数据可评估）
- ams下发的接口（数据结构，核心字段有哪些，各个字段的填充比例，填充长度，有哪些指标数据可评估）
- 数据表现：分维度（触发来源、广告库、模板、计费类型等）的广告表现（相关性、ctr、cpm） 确认这些数据是否有
- bidword到title映射（title集合（来源，更新机制等），title截断逻辑（考虑哪些维度的信息），还有哪些指标数据可评估）
- 设计：在QB百度搜索query-title日志训练dssm模型结构基础上，新增masked language model、句式分类、主题分类
- adposcount=1&posid=1080416493270959&count=10&datatype=2&charset=utf8&datafmt=json&uin=2444980824&ext={"req":{"guid":"6e8e002882991d9ad98850a313b788cb","c_cd":32,"deep_link_version":0,"hostver":"4000","qbdebug": true,"c_mf":"MI9","c_os":"android","c_osver":"9","lat":22537342,"lng":113939536,"carrier":2,"conn":1,"muid":"0515334c81271dedd0e677894af4fd26","muidtype":1},"pos":{"0":{"query":"小7手游","suggests":["小七手游:259"]}}}
- 补全比例：直达请求query 64%有召回
- 句式分类（判断一个句子是否为问句）：众包标注20w
- item特征
- 预估服务（苏展）
- 202006策略梳理
- 预估数据来源：搜索直达
- 需要对比搜狗搜索的广告：
- 主题分类：qb浏览器资讯title分类数据
- 主要内容：发送分词后的“用户原始query”和“补全sug”
- 来源：直投广告top1000广告+复用广告已在直达区全量广告的购买词

## 与推荐/广告/数据科学面试的关联
- 召回策略（向量/图/协同过滤/多路）
- 排序模型（Wide&Deep/DIN/ESMM/树模型）与重排
- CTR/CVR 预估与校准
- 用户画像/分层/生命周期
- 特征工程/embedding
- AB 实验与评估框架
- 转化漏斗与渠道效能

## 关键术语
AMS、CTR、CVR、QBBusinessRewriteObj、QBBusinessRewriteServer、RNN、ROI、SUG

## 与本人项目的关联
百度、通用推荐广告（文中出现相关关键词）

## 价值评估
**高** —— 与本人项目（百度、通用推荐广告）直接关联，且含方法/指标/术语

