# 信息流搜索推荐服务重构 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/看点搜索/搜索推荐组/信息流搜索推荐服务重构.doc` ｜ 3395字

## 一句话定位
关于QQ看点/图集、通用推荐广告相关的技术资料（《信息流搜索推荐服务重构》），含 24 条关键要点。

## 核心内容摘要
当前快报的搜索词推荐正在实验阶段，且代码流程由 QB 服务删减迁移而成，CTR 模型也是复用 QB 模型，因此计划以这一份共同代码为基础实施重构；// CTR/CVR 预估等后续流程

## 结构大纲
- 新服务代码的设计原则为：
- 一个典型的控制流如：
- 而对于一个服务接口要做的事情是：
- 一次典型的请求处理流程形如：

## 关键方法 · 模型 · 指标
- 当前快报的搜索词推荐正在实验阶段，且代码流程由 QB 服务删减迁移而成，CTR 模型也是复用 QB 模型，因此计划以这一份共同代码为基础实施重构
- // CTR/CVR 预估等后续流程
- 相同场景使用同一套策略和模型，通过配置参数、模型特征适配不同应用的特殊性
- 先完成基本的代码重构，包括通用的服务架构和共有功能，部署替换现有的快报推荐服务
- 由于看点代码和策略和前两者有一定距离，这一步不能保证完全等效，但要评估保证效果近似且不变差
- 即使相同场景下，不同应用的词库、处理流程、模型等也各不相同，导致日常支持维护、定位问题、升级优化的成本很高
- // 更多召回
- 编写基础架构代码
- 信息流搜索推荐服务重构
- 长期目标：策略层面的统一
- 定义接口，编写代码框架，迁移基本功能到新框架
- 看点策略不同点较多，需视上述效果安排迁移计划
- 异步调用破坏了控制流的线性关系，调用关系表现为依赖图
- 因此计划重构一个统一的搜索词推荐服务，兼容多个应用的多个场景
- 快报全量，QB 评估和线上 diff 可接受后切流量观察效果
- 在接收到请求时按控制流图组装 Processor，然后发起一次异步调用
- QB 正文底部和快报正文底部使用的实时曝光上报的 DCache 数据结构也不相同
- 但是推荐服务调用其他服务的时候，通常使用异步调用的方式来提高性能，即执行调用的线程和处理返回结果的线程不是同一个
- 上图一个功能模块实现为一个 Processor，Processor 的输入输出参数实现为 ProcessorParam
- 比如说，QB 正文底部的文章主题和 tag 是推荐服务主动从 DCache 获取的，而快报的主题和 tag 是上游使用参数传下来的
- 功能模块与业务逻辑解耦，语义召回、质量过滤等作为功能模块提供独立可重用的功能，针对特定场景的需求可以简单组合任意功能模块，再加上少量的特定开发
- 信息流搜索推荐当前支持 QQ 浏览器、手机 QQ 看点、看点快报3个应用，视应用不同又有正文底部、feeds 流回退、搜索结果页、搜索结果页回退等多种场景
- 如下为新服务架构的服务间调用关系，需要注意的点是由于 QB 和看点已经有各自不同的历史接口在线上运行，所以需要额外的适配代码把对历史接口的调用转换到新通用接口
- 所有场景共用一套代码，尽力做到不同应用的相同场景（如：QB 正文底部和快报正文底部）主要流程相同，在此基础上用分支、适配的形式兼容线上现有的不同词库、业务逻辑、模型

## 与推荐/广告/数据科学面试的关联
- 召回策略（向量/图/协同过滤/多路）
- CTR/CVR 预估与校准
- 特征工程/embedding
- AB 实验与评估框架

## 关键术语
CTR、CVR、DCache、QB、QQ、SERVERSUCCESS

## 与本人项目的关联
QQ看点/图集、通用推荐广告（文中出现相关关键词）

## 价值评估
**高** —— 与本人项目（QQ看点/图集、通用推荐广告）直接关联，且含方法/指标/术语

