# 【Query召回之D2Q端到端语义检索召回】项目说明文档_V1.0_20200425 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/看点搜索/搜索推荐组/【Query召回之D2Q端到端语义检索召回】项目说明文档_V1.0_20200425.docx` ｜ 1297字

## 一句话定位
关于通用推荐广告相关的技术资料（《【Query召回之D2Q端到端语义检索召回】项目说明文档_V1.0_20200425》），含 22 条关键要点。

## 核心内容摘要
从文章曝光到给出推荐词，隐式召回的处理流程最长可到1h，时效性存在提升空间；资讯文章具有较高的曝光时效性，精准、快速地召回相关query并推荐给用户是非常必要的

## 结构大纲
- 技术实现分为两期：一期为Title+Tag，二期引入abstract等Doc信息

## 关键方法 · 模型 · 指标
- 从文章曝光到给出推荐词，隐式召回的处理流程最长可到1h，时效性存在提升空间
- 资讯文章具有较高的曝光时效性，精准、快速地召回相关query并推荐给用户是非常必要的
- 资讯文章底部以及返回的query词推荐(精探)，作为一种被动搜索推荐方式，能够吸引用户拓宽搜索，有力提升用户消费时长
- 文章推荐的每日线上点击相对提升10%
- （备注：二期Doc Graph Embedding）
- 通过源文章到目标文章的匹配迁移，将候选文章对应的高点击query召回
- 通过对文章的核心词/句的分析提取，在推荐池中召回相关query，并通过线上的Query-Click训练的MaskDssm模型进行相关性过滤
- 点击or曝光的label，doc_id/ doc_title/ doc_title/ doc_class/ doc_tag/ query/ query_pos等
- DSSM模型架构—Bi-LSTM RNN
- 模型训练：基于线上的query推荐的点曝数据进行训练（随语料定期finetune)
- 用户数据形态
- D2U端到端语义召回方案
- D2U端到端语义召回落地
- D2U端到端语义召回目标
- 核心词句的特征抽取环节是解耦的，并非端到端的服务形式
- 文本表示representation（如图：蓝底部分）
- “Query召回之D2Q端到端语义检索召回”说明文档V1.0
- 技术实现分为两期：一期为Title+Tag，二期引入abstract等Doc信息
- Title/Sentence->word：tf-idf生成词权、TWE、ctr预测权重
- 一期：先利用title+tag数据，基于Query-Title训练的模型进行finetune
- Taf服务：搭建D2U端到端语义召回服务(similarity、Vectorization)
- 目前Doc to Query（D2Q）的召回方式有多种，包括热词、关联词、核心词、核心句（Review/E2q/Hot/DocSim/AsooWord/NLP/NLPSeman）等，其中隐式召回方式主要是基于文章相似度、核心词(句)

## 与推荐/广告/数据科学面试的关联
- 召回策略（向量/图/协同过滤/多路）
- CTR/CVR 预估与校准
- 用户画像/分层/生命周期
- 特征工程/embedding

## 关键术语
CTR、DBOW、DIN、Embedding、LSTM、NLPSeman、PV、RNN、TWE、Word2Vec

## 与本人项目的关联
通用推荐广告（文中出现相关关键词）

## 价值评估
**高** —— 与本人项目（通用推荐广告）直接关联，且含方法/指标/术语

