# 浏览器严口径数据完全说明 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/看点搜索/数据平台组/浏览器严口径数据完全说明.doc` ｜ 39216字

## 一句话定位
关于QQ看点/图集、通用推荐广告相关的技术资料（《浏览器严口径数据完全说明》），含 30 条关键要点。

## 核心内容摘要
主动打开 ：也称主启，自启，都是指用户自发地使用浏览器，但是，在Android系统眼里，点击图标和APP切换，都是等同的主动打开，而如前所诉，APP切换是可以返回文件子进程的；当前（2020年6月），严口径是指Android浏览器排除文件子进程场景和系统浏览器后剩余的活跃用户数

## 关键方法 · 模型 · 指标
- 主动打开 ：也称主启，自启，都是指用户自发地使用浏览器，但是，在Android系统眼里，点击图标和APP切换，都是等同的主动打开，而如前所诉，APP切换是可以返回文件子进程的
- 当前（2020年6月），严口径是指Android浏览器排除文件子进程场景和系统浏览器后剩余的活跃用户数
- 严口径全量用户表，用于计算新增，活跃，留存
- 严口径启动次数 ：Android基于时长日志本身提供的sessionid来对单个用户计算四大类打开方式的次数
- 可以点击返回按钮返回微信聊天框，通过Android系统的应用切换界面，可以看到，该界面与微信进程，浏览器主进程是独立的
- 由前一节可知，严口径主要是提出文件子进程和系统浏览器，而文件子进程，唯有时长日志才能够区分，因此Android的严口径SQL如下：
- 表3的处理：将Android和iPhone数据合并，形成完整的严口径DAU原始数据，并带有打开方式及其次数统计，带有单用户当日总时长
- 距离2，用户在系统浏览器和主线浏览器各停留10分钟，且guid是互通的（系统浏览器与主线浏览器通过SD卡共享guid），则它的严口径使用时长为20分钟
- 严口径使用时长 ：Android严口径实际是用户行为达标，而时长是对用户注意力的占用，因此严口径使用时长，是严口径DAU用户当天的所有时长（含文件子进程时长）
- 严口径打开方式 ：Android按照大盘启动方式四大类拆分，但基于时长日志来对行为分类，仍然分成主动，通知栏，PUSH和外部调用，但需要去掉无效的主动和无效的外部调用
- IdMTR28HQ6FOAyya9W3TGVKSDR/nAAhlZcqNoGizab50I+h3a/2HxwBJSQ5Vvv9fVn5No/L7QFLi
- 后续会通过时长上报优化该现状
- iPhone用户严口径DAU的打开方式和总使用时长
- iPhone用户严口径DAU的见首页状况和停留时长
- 主动打开不一定是严口径用户，它可能打开了系统浏览器或者返回文件子进程
- Android用户行为标签化预处理数据，能同时产出严口径打开方式和见首页数据
- 表5中，Android用户一定是见过首页的，而iPhone用户则没有做此限制
- 这里肯定会有一个疑问，为什么Android的算法和iPhone的算法如此的不一致
- 见首页使用时长日志来区分，Android直接使用，iPhone使用关联上的时长流水
- 3.时长日志只能覆盖MTT6.6以上的版本，但我们仍然有少量低于MTT6.6的存量用户
- 严口径活跃，是一种对活跃行为的要求，而不是宽口径活跃行为中，曾经达标严口径的那部分用户
- 表1的处理：没有抛弃非严口径的用户，而是将其标签化，方便分析宽口径转严口径的漏斗和路径
- 参考guid口径归因后的全量用户表，mtt.t_ad_mtt_all_user_guid
- 严口径周活跃天数 ：如果一个用户，周1,3,5,7用主线看信息流，而周2,4,6用白牌看信息流
- 严口径首启方式 ：Android取严口径用户的严口径行为中，日志时间最早的那一条日志，作为首启方式
- 表6的处理：将Android和iPhone数据合并，形成完整的见首页原始数据，并带有见首页的方式和时长
- 如论及最严口径用户的人均时长，建议iPhone平台是时长和DAU分开算，不要求时长的用户必须是DAU用户
- 文件子进程 ：举一个例子，Android平台微信和浏览器共存时，微信聊天框中打开文档，会以子进程的形式来服务用户
- 业务严口径用户 ：以搜索或资讯为例，他们都可以在系统浏览器提供服务，但文件子进程，则绝对没法提供信息流和搜索服务
- 可以关掉它而不影响微信或者浏览器主进程使用，可以点击系统多窗口返回该子进程，当然，关掉浏览器主进程也不影响该子进程

## 与推荐/广告/数据科学面试的关联
- 冷启动（EE/元学习/内容特征）
- CTR/CVR 预估与校准
- 归因与因果推断（PSM/DID/AB）
- 用户画像/分层/生命周期
- 留存/时长/活跃 指标与因果
- 转化漏斗与渠道效能

## 关键术语
AND、AS、AYVVv、CBaNUAKMylBx、CNN、COUNT、CR、CTR、CVR、DFtpvuWLZ、DISTINCT、DKVyMjh、ECDIxRm、EKY、EZoPlty、FROM、GRU、GUpFNj、HKV、HZ、IF、IJw、IJzcP、IN、INSERT、JRoP、JVIz、KER、KS、LIKE

## 与本人项目的关联
QQ看点/图集、通用推荐广告（文中出现相关关键词）

## 价值评估
**高** —— 与本人项目（QQ看点/图集、通用推荐广告）直接关联，且含方法/指标/术语

