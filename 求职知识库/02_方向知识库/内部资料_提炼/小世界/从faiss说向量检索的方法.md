# 从faiss说向量检索的方法 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/小世界/从faiss说向量检索的方法.pdf` ｜ 7709字

## 一句话定位
内部资料《从faiss说向量检索的方法》，属技术文档，提取 30 条关键要点供复习索引。

## 核心内容摘要
以图搜图、推荐系统、等等；IVFPQ 框架图

## 结构大纲
- 可以让你了解到：
- 1. 倒排索引的设计思路。
- 2. ⾼效检索系统的实现 idea 。
- 3. ⾼维降维，向量编码。
- 4. 主成份分析⽅法。
- 5. 体会课堂知识如何应⽤于成熟项⽬中
- 1. Faiss 背景介绍
- 2. Faiss 包含的内容
- 输出：矩阵 。即距离 最近的 个向量结果。
- 1. 构建训练数据
- 2. 选择合适的 Index
- 3. 加⼊ Index 中训练
- 4. 进⾏搜索
- 1.  距离：欧⽒距离（ Euclidean ）
- 2. 距离：内积距离（ Inner Product ）
- std::vector<std::vector<uint8> > codes
- std::vector<std::vector<uint8> > ids
- 算法：
- 1. 初始化：随机选取 个质⼼点
- 2. 分配：每个点分配到最近的质⼼点对应的集合中
- 3. 更新：每个集合重新计算质⼼
- 4. 重复迭代多次
- IndexIVF: 倒排⽤ KNN ，只是基类
- 量化：信号处理领域将连续取值近似为有限多个离散值的过程
- 训练：找到 的过程

## 关键方法 · 模型 · 指标
- 以图搜图、推荐系统、等等
- IVFPQ 框架图
- 4. 计算特征值和特征向量
- K-means （ KNN ）  聚类算法
- 从⼀个完全没接触过算法的⼩⽩⻆度，以 faiss 为标准，讲解向量检索的背景和常⽤的实
- 输⼊：向量  ，
- （和倒排的聚类中⼼
- 素对点击率影响最⼤
- 其它的向量变换⽅法
- 什么是  向量相似检索
- 整个向量 的编码结果为
- 3. ⾼维降维，向量编码
- 给定向量集合 （数据库）
- 哈希函数和输⼊数据不匹配
- 3. 基于背后的论⽂算法
- 即距离 最近的 个向量结果
- 距离计算的图示 (*) ：
- 中最  有意思 的⾏为特征
- 基本过程类似，有个别优化的地⽅
- HNSW ：基于图的检索⽅式，
- hint 1 宏定义打开编译优化
- 优化点：原来距离计算是使⽤  ，
- 2. ⾼效检索系统的实现 idea
- 看懂算法才能看懂代码，并⽆其它特别之处
- x, centroids.data(),
- 暴⼒搜索 标量量化 ⽮量量化 图查询 哈希
- 向量变换， VectorTransforms
- 向量量化  IndexPQ "PQ16x8"
- 向量变换  VectorTransforms
- 会对⾼维特征使⽤ PCA 等降维⽅法对特征做降维处理

## 与推荐/广告/数据科学面试的关联
- 特征工程/embedding

## 关键术语
ANN、AU、CM、CPU、HNSW、IDMap、IP、ITQ、ITQOPQ、ITerative、IVF、IVFPQ、IVFs、KM、KNN、LSH、LSHr、LSHrt、LSHt、OPQ、PCA、PCAMatrix、PCAR、PCAW、PCAWR、PQ、ROI、RR、SIMD、SQ

## 与本人项目的关联
无明显关联

## 价值评估
**中** —— 含部分方法/指标要点，可按需查阅

