# 迁移学习调研 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/小世界/迁移学习调研.pptx` ｜ 3752字

## 一句话定位
关于通用推荐广告相关的技术资料（《迁移学习调研》），含 30 条关键要点。

## 核心内容摘要
估计方法是将源域数据和目标域数据进行合并，使用LR模型训练一个两类的分类器，将分类器的得分作为对它的一个估计值；6. How transferable are features in deep neural networks?

## 结构大纲
- 实例迁移：TraAdaboost[3]
- 训练集两部分：
- 1.和测试集分布相同的少量训练数据 Xs
- 2.和测试机分布不同但是数据量较丰富的训练数据Xd
- 权值定义：
- A和B：两个不同类型的数据集
- 比较：
- 结论：
- 迁移效果下降原因有二：特征自身specificity的问题 ，特征之间是耦合的
- 三部分：特征提取器，标签预测器，域分类器，梯度逆转层
- 数据：源数据（有标签），目标数据（无标签）
- 损失：
- 思考：
- 2. agent 使用自身的policy function生成下一个动作at。
- 4. Critic进行执行动作 at所能获得的未来收益的期望的预估。
- 2. A Survey on Transfer Learning
- 3. Boosting for Transfer Learning

## 关键方法 · 模型 · 指标
- 估计方法是将源域数据和目标域数据进行合并，使用LR模型训练一个两类的分类器，将分类器的得分作为对它的一个估计值
- 6. How transferable are features in deep neural networks?
- 三部分：特征提取器，标签预测器，域分类器，梯度逆转层
- 通过TraAdaboost 和样本权值预估进行源数据样本权值的预估
- 不同场景的商品排序仅考虑自身，会导致用户的购物体验是不连贯或者雷同的
- 迁移效果下降原因有二：特征自身specificity的问题 ，特征之间是耦合的
- Pre-training+Fine-tuning的效果可以有效提升模型性能，而且不需要指定具体迁移多少层
- 引入一个新的预测分支，两个预测分支分别预测不同的数据，同时保留域分类器使得前面的特征提取层学习到更加一般的特征
- 估计放方法是直接将两个数据进行合并训练一个预测模型，然后将模型对于每个样本的预测得分到决策边界的距离作为它的估计
- 针对Xs中的数据（n+1到m），也就是和测试集分布相同的训练数据，每次循环和普通的adaboost类似
- 对比实验说明
- 迁移学习的分类
- 多场景联合优化
- 传统的单场景优化
- 样本权值预估[4]
- 迁移学习的分类[2]
- 基于深度的特征迁移[5]
- （2）域分类器损失：交叉墒损失
- 评估的是样本x在目标域中的重要性
- base A：A上训练的神经网络
- base B：B上训练的神经网络
- 平衡的交叉熵损失(考虑正负样本)
- 通过聚类将多个任务进行合并[10]
- 实例迁移：TraAdaboost[3]
- 主搜索与店铺内搜索联合优化的初步探索与尝试[16]
- 评估的是任意一个来自源域的样本，他对于目标域的相似性
- 如何衡量具体某一层的特征是general和specific
- 各个场景都展现趋同，都包含太多的 U2I（点击或成交过的商品）
- 4. Critic进行执行动作 at所能获得的未来收益的期望的预估
- 基于Pre-training+Fine-tuning的深度学习迁移方式

## 与推荐/广告/数据科学面试的关联
- 排序模型（Wide&Deep/DIN/ESMM/树模型）与重排
- 用户画像/分层/生命周期
- 特征工程/embedding
- AB 实验与评估框架

## 关键术语
DIEN、KS、PLE

## 与本人项目的关联
通用推荐广告（文中出现相关关键词）

## 价值评估
**高** —— 与本人项目（通用推荐广告）直接关联，且含方法/指标/术语

