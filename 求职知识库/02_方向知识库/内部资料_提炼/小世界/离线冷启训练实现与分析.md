# 离线冷启训练实现与分析 · 提炼笔记

> 来源：`01_原始资料库/03_工作资料/工作资料_腾讯/2020年09月-内部资料/小世界/离线冷启训练实现与分析.doc` ｜ 2311字

## 一句话定位
内部资料《离线冷启训练实现与分析》，属技术文档，提取 12 条关键要点供复习索引。

## 核心内容摘要
$w = (1, 0, 0, 1, 10, 1)$ 模型不收敛（loss 不下降）；reward 定义为 (auc, mean_watch_duration, mean_duration, click_propotion, like_propotion, baseline_score) 与权重向量 $w$ 的内积

## 关键方法 · 模型 · 指标
- $w = (1, 0, 0, 1, 10, 1)$ 模型不收敛（loss 不下降）
- reward 定义为 (auc, mean_watch_duration, mean_duration, click_propotion, like_propotion, baseline_score) 与权重向量 $w$ 的内积
- 约 2/3 的状态下会选择该策略，其他的选择也会比较接近线上策略
- 从目前观测到的离线实验来看，reward 权重的选择对结果有较大的影响
- if (reward.auc < baselineReward.auc) {
- if auc < baseline.auc: baseline_score -= 1
- 修改为 $w = (1, 0, 0, 1, 10, 0)$ 后，算法输出的结果相对正常
- $w = (1, 0.01, 0.05, 1, 1, 0)$ 时，算法倾向于选择 alpha 比较小的 action
- $w = (1, 0, 0, 1, 10, 0.2)$ 会有较明显的选择当前线上策略 (6.2, 2.8, 0.8, 0.34) 的趋势
- 此时约有 1/3 的状态选择了线上（即 baseline）策略，1/2 的状态选择了另一种策略 735: (7.5,3.5,1.0,0.34)
- $w = (1, 0, 0, 1, 10, 0.2)$ , 模型训练时 loss 波动极大，且几乎所有 state 预测的 action 都是线上的参数
- DQN 大致参照了 https://medium.com/analytics-vidhya/building-a-powerful-dqn-in-tensorflow-2-0-explanation-tutorial-d48ea8f3177a 的实现，但把其中的卷积层替换成全连接层

## 与推荐/广告/数据科学面试的关联
- AB 实验与评估框架

## 关键术语
AUC、DIN、DQN

## 与本人项目的关联
无明显关联

## 价值评估
**中** —— 含部分方法/指标要点，可按需查阅

